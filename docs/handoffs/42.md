# 交接 42 — backlog 清理会话（⑧ NOTIONAL 修复 / ⑤ far-limit 收 src / ⑥ 裁定为文档轮）

> 本会话 = backlog 续清。承 41.md（HEAD a8f4270，baseline 712）。
> 净产出：⑧ resolved（9fe2bb0）+ ⑤ resolved（868ac26）。
> baseline 712 → 729，HEAD a8f4270 → 868ac26，工作树 clean，与 origin/main 同步。

## §0 背景定位
- 承 41.md。本会话挑 backlog「二、文档债 8 项」中可做项续清。
- 三方协作模型不变：Iris 主理人（唯一 git 写权）/ Web Claude 复核出题 / Claude Code 执行。
- 进度以仓库 git log 为最终准，文档次之（沿袭 22/23.md 铁律）。

## §1 纪律红线（承 41.md 全部条款）
- STOP-DOCS / STOP-ARCH / STOP-VERDICT / STOP-B 不变。
- ce9 五基线 md5 零漂移哨兵：guard / precision / nt_translate / nt_submit / exec_bridge。
- 本会话首次正式移动一个 ce9 基线（nt_translate，见 §2.1）——授权前提下的合规迁移。

## §2 本会话核心产出

### §2.1 ⑧ filters 接缝 → confirmed ce9 bug 修复（9fe2bb0 ✅）
- 调查链：⑧ 原标「test 热身」→ recon 揭出非 test-only → 联网实测 testnet+mainnet
  对 SOLUSDT/ETHUSDT 均返回 filterType "NOTIONAL"（非 MIN_NOTIONAL）→ 确认 ce9 bug。
- 根因：nt_translate.from_info 写死 _MN="MIN_NOTIONAL"，真数据上 ValueError；
  且把 MIN_NOTIONAL 类型与 applyMinToMarket（实为 NOTIONAL 字段）错配。
- 修法（N carve-out，仅 nt_translate.py）：认双名 NOTIONAL+MIN_NOTIONAL，按类型取对
  apply 字段（NOTIONAL→applyMinToMarket / MIN_NOTIONAL→applyToMarket）；改正证伪的
  _SOL_FILTERS fixture（→ 真 NOTIONAL 六字段）；加 CA7 双形态回归。
- 休眠性：nt_translate 在 src 零 import、零 caller，纯 test-only 死代码，当前不伤实盘。
- **ce9 基线迁移**：nt_translate.py md5 045be165 → a9cd0219475a99c6b1ea145d4e45f370。
  test_post_only_routing.py 的零漂哨兵基线已同步更新（in-place 单行替换，CE8 核过）。
  **旧 docs（24–38）里的 045be165 全部作废，以 9fe2bb0 为准。**

### §2.2 ⑤ far-limit 算法收进 src（868ac26 ✅）
- 新建 src/hermes/execution/far_limit.py（非 ce9）：compute_far_limit_buy()
  采生产路径路线 ppbs_lower * (1 + safety_margin) 再 floor_to_tick；
  safety_margin 可配，默认 Decimal("0.001")；import precision.floor_to_tick，零 ce9 改动。
- 设计裁定：三份探针副本算法不等价——wp8 贴边界（无缓冲）vs cb0/phase1 +0.1% 缓冲。
  取 cb0/phase1 生产路径路线（防 PPBS 边界拒单）；wp8 贴边界 = margin=0 的特例。
- experiments/ 探针副本（stopb_cb0_production_path / stopb_phase1_cancel / wp8）由本模块取代。
- 未接 live（新模块无 caller，零 live 行为变化）。15 单测全绿。
- 新模块 md5：far_limit.py = e8b586ff74639724f24285d289cc4a38
  test_far_limit.py = 68c99855e021e96d5b6b44d1a286e1ee

### §2.3 ⑥ MasterTracker v4 → 裁定为文档轮（本会话不做）
- recon 实证：MasterTracker 是战略规划文档（v2/v3），非代码模块；src/tests 零命中。
- 已被 22/23.md 判定「仅供战略参考，不作进度基准」「以 git log 为准」。
- 裁定：⑥ = A（重写一份对齐仓库现实的 v4 规划文档），与 ① CLAUDE-REBUILD 同属纯文档大件，
  留专门文档轮（fresh session）做。
- **关键发现**：v2/v3 完整规划正文不在仓库（MTV3 仅命中 20/22/23.md 的标题+冲突注记）。
  → v4 基本是从零写对齐现实，非承接 v3 重对齐。

## §3 当前手册锚 / baseline
- baseline：729 passed（41.md 时 712 → +2 NOTIONAL CA7 → +15 far_limit = 729）。
- HEAD：868ac26，与 origin/main 同步，工作树 clean。
- ce9 五基线 md5：guard b93da5a2 / precision bb0308ce / nt_submit db974054 /
  nt_translate **a9cd0219**（本会话迁移）/ exec_bridge（未核，沿袭）。
- 若 CLAUDE.md 旧锚仍写 590/712 请替为 729。

## §4 当前 src 模块全景（git log + find 实证）
- execution/：binance_exec_client / events / exec_bridge / exec_reconciler / far_limit(新) /
  nt_order_translate / nt_submit / nt_translate / oneshot_strategy / order_id / precision / seen_store
- risk/：guard / risk_state_store
- strategies/：sma_crossover / v25_trend
- 空骨架目录：backtest / monitoring / orchestrator / regime（占位，未落地）
- ⚠️ 待核口径：execution/ 下 nt_translate.py 与 nt_order_translate.py 两个 translate 模块分工需澄清（v4/① 时核，记 backlog 候选）。

## §5 backlog 8 项现状
- ① CLAUDE-REBUILD-FULL（§1-5/§8 重建）— ❌ 文档大件，留文档轮
- ② LC-XPLAT-1（fcntl/Windows）— 🟡 risk_state 解 / seen_store 据暂缓
- ③ LC-RISK-4（atomic save）— ✅ resolved（b97b71d）
- ④ _daily_pnl 持久化边角 — ❌ 卡 ce9（guard.py）+ 需设计裁定
- ⑤ far-limit 收 src — ✅ resolved（868ac26，本会话）
- ⑥ MasterTracker v4 — 📋 裁定为文档轮 A，留新会话（本会话）
- ⑦ 洞2 notional raw_price — 🔬 查实未修，并 F 待 ce9 carve-out 会话
- ⑧ filters 接缝 — ✅ resolved（9fe2bb0，本会话）
汇总：✅③⑤⑧ / 🟡② / 📋⑥ / 🔬⑦ / ❌①④

## §6 governance 记录（本会话）
1. ce9 基线首次合规迁移：nt_translate 045be165→a9cd0219，授权于 N carve-out；旧 docs 锚作废。
2. **CC 程序违规**：⑤ 的 T7 单测失败后，CC 自行改断言方向（<= → >=）未报 STOP-VERDICT，
   后经复核裁定方向正确（合理收紧非放宽）、CC 已自申。
   **约束写死：测试失败后断言方向/阈值变更须先报 STOP-VERDICT，不得自行改后继续。**
3. far_limit 新模块基线记录在案（§2.2），非 ce9、未接 live。

## §7 ⑦+⑧ 合流后续（⑧ 已修，⑦ 仍 open）
- ⑧ 的 nt_translate NOTIONAL 修复已收口（本会话）。
- ⑦（market-order notional raw_price，precision.py L231 raw_price if is_market）仍未修。
- F（nt_translate 接线进 live + instruments.py precision=2 硬编码替换 + ⑦）= 架构级，
  需独立 ce9 carve-out 会话 + 设计裁定，紧挨 go-live 前必做。

## §8 STOP 协议速查（沿袭）
- STOP-B：真 testnet 单前
- STOP-DOCS：文件改动前（diff 全文 → 复核签 → md5 preview → go → write → 核 md5）
- STOP-ARCH：结构/架构改动，须指明 carve-out 精确范围
- STOP-VERDICT：任一 verdict 条件失败 / 断言变更（见 §6.2 新增约束）

## §9 下轮起手清单
- 文档轮（①+⑥ 合并）：开局读本 42.md → 用 §3/§4/§5 现实快照对齐 → 与主理人定 v4 粒度
  （a 战略路线图 / b 完整操作手册）→ 复核出题方对齐骨架 → 成段文本 → 主理人落盘。
- ce9 carve-out 轮（④+⑦+F）：攒一轮批处理，避免反复动基线文件。
- 现实快照（本会话采，下轮直接用）：HEAD 868ac26 / baseline 729 / src 模块全景见 §4。