承接：docs/handoffs/35.md（b3b-live 发单入口入库，锚 16eedd0 → 落档后 HEAD=f2a86c9）。本文档为任务中途交接（非干净收口），供新会话无缝接手。当前有两条并行线，下面分别钉清。

0. 背景定位

项目：hermes —— 基于 NautilusTrader（NT 1.227）的 Binance 自动交易系统
本会话起于「等 testnet 真单期间推进 M2 策略」，实际推进了两条线：

线 A（最高优先，M3 收口）：b3b-live 第一笔 testnet 真单 —— 阻塞于 testnet 502，未发出，账户未动
线 B（等待期工作）：M2 V25 策略移植一期 —— 推进到 Step 2 完成、策略类待复核


当前态 = 两线均中途，工作树 clean，HEAD=f2a86c9，无新 commit


1. 红线（承 CLAUDE.md / 35.md，不变）

绝不 git add/commit/push（CC 被 GATE-1 物理拦，Iris 手敲）
STOP-DOCS：预览先行 + 预览 md5 == 写入 md5；写后等显式 go 才 cp
STOP-B：连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐
真单只由 Iris 手敲 trade-live（去 --dry），TRADE-GATE 物理拦 CC；复核方不替起头、不驱动 CC 发单、绝不找闸字面串绕过（P2 红线）


2. 角色与流程约定（新 AI 必读）

三方：CC（Claude Code，本机跑文件编辑/测试；git-write 被 GATE-1 物理拦）/ Iris = 主理人 = 用户（唯一 git 写入 + 唯一真单执行主体）/ 复核方（本对话角色：出 PLAN/判据、复核 raw、签 CLEAN，不碰执行）
节奏：recon → PLAN（复核方）→ Iris go this plan → implement（CC）→ 验证 raw（CC）→ 复核签 CLEAN → Iris 手敲 commit
raw 纪律（§10.5 / §6.4.3，本会话反复用到）：CC 的「done / 已移植 / CLEAN」是 verdict，cat/git/pytest 输出才是 raw。复核方对存在性/逻辑断言一律以 raw 为准，不取转述。CC 自报的清单/表格不能替代代码逐行核。
分段验证节奏：复杂移植按 Step 切，每 Step 做完 STOP 等复核方过眼，再放下一 Step（checkpoint 工作流）


3. 当前基线快照

git 锚：origin/main = f2a86c9（= 16eedd0 b3b-live 入口代码 + 35.md 落档）；工作树本应 clean，但本会话新增两个 untracked 文件（见 §5）
链：f2a86c9 → 16eedd0 → 701323c → 85fd1da → aec6bd8 → ce3204f
baseline：692 passed（本会话未跑全量，移植 Step 未完，零回归待 Step 6 验）
三闸指纹（用户级 ~/.claude/，环境重建须重施）：git-write 281c070d / mainnet 02ed4b83 / trade c9c4cbb0 / settings 625c1a96
三闸行为：本会话会话启动复验全 CLEAN（GATE-1 deny+HEAD未动 / GATE-3 deny非crash / GATE-2 deny+testnet放行；raw 实测）
ce9 四基线 md5（动前必查）：precision bb0308ce / nt_translate 045be165 / nt_submit db974054 / guard b93da5a2 —— 本会话复验四行逐字对上


4. 线 A — b3b-live 真单（最高优先，阻塞外部）
状态：三件套齐，差 testnet 恢复。本会话 Iris 已两次手敲，均在发单前因 testnet 502 崩溃，单未发出、账户未动。

① 发单闸 CLEAN —— ✅ 本会话 GATE-3 重验活
② Iris STOP-B —— ✅ 本会话 Iris 明确「续效 + 直发」（接受跳过 --dry 干跑的代价）
③ J1–J6 判据 —— ✅ 在档（见 35.md §4）

真单参数（STOP-B 锁定，逐字不得偏离）：
hermes trade-live --symbol ETHUSDT --side BUY --price 1300 --qty 0.0039
ETHUSDT / BUY / LIMIT / 1300 / 0.0039 / TESTNET；名义 1300×0.0039 = 5.07 > minNotional 5 ✅；1300 远低于现价 → 验「挂进去」非真成交。
502 性质坐实：本会话探活 ping/exchangeInfo/ticker/avgPrice 全 502，mainnet ping=200 → 单边 testnet 故障，非出口网络问题、非代码/参数问题。崩溃点在 cli.py:152 resp.raise_for_status()（发单前拉 exchangeInfo 那步），远在 submit_order 之前。
恢复判断（唯一动作）：
curl -s -o /dev/null -w "ping=%{http_code}\n" "https://testnet.binance.vision/api/v3/ping"
回 200 → testnet 活，立即停下线 B，Iris 逐字重敲上面那条 trade-live（参数不变，STOP-B 仍续效，无需重授权），打完贴 J4 四样 raw（orderId / status 预期 NEW / 回执全文 price·origQty==1300·0.0039 / node testnet 连接日志）给复核方核 J1–J6。仍 5xx → 继续等，不连发硬撞（J5 纪律）。

5. 线 B — M2 V25 策略移植一期（推进到 Step 2 完成，策略类待复核）
5.1 任务与范围（一期）
把 V25 趋势策略移植成 NT Strategy 子类，复用 wp4 回测管线、改读本地 data/SOLUSDT_2024_1h.csv（离线，与 testnet 解耦），跑通 BacktestEngine 出 PnL。

一期纳入：EMA20/50/100、ATR14、ADX14、突破入场、金字塔加仓（add_count 分级 + ADX>40 连阳≥4 dominance）、多级止盈（8/15/30%）、ATR/回撤移动止损、连亏冷却（rl≥3→cd=24）
一期排除（二期再做，禁止混入）：Hermes Bank 利润提取（模块B）、rm 回撤>25%降半仓、pk/dd 资产峰值追踪、m15/m20 里程碑、Monte Carlo

5.2 PLAN（已 Iris go this plan，6 步）

Step 1 ✅ 已 CLEAN：CSV→Kline 加载器（experiments/wp_v25_backtest/main.py）
Step 2 ✅ 已完成，待复核：策略类（src/hermes/strategies/v25_trend.py，392 行）
Step 3 ⏳：引擎接线（照 wp4：add_venue NETTING/CASH/10000 USDT + add_instrument + add_data + add_strategy + run + 取 PnL）
Step 4 ⏳：跑回测出 PnL
Step 5 ⏳：对拍原版基准
Step 6 ⏳：全量 pytest 692 零回归 + ruff + mypy

5.3 本会话新增的两个 untracked 文件（gitignore 覆盖 experiments/，src 那个待 Iris 决定何时 commit）
文件状态备注experiments/v25_reference/v25_original.pyuntracked（gitignore experiments/*）V25 原版完整源，175 行，md5=0df5f745，SYNTAX_OK + 跑通（参考基准源，不进 src/git）experiments/wp_v25_backtest/main.pyuntrackedStep 1 加载器，已 CLEANsrc/hermes/strategies/v25_trend.pyuntracked（?? ）Step 2 策略类，392 行，AST_OK，待复核，未 commit
5.4 Step 5 对拍基准（V25 原版同数据实测，已钉死）
原版 python experiments/v25_reference/v25_original.py 实测：
数据：8784 行原始 → 8757 行（指标 dropna 后）
清算次数 = 13
胜率 = 76.9%
总资产 = $11287.96（+12.88%）
Bank 落袋 = $0.00（未触发：全年仅 +12.88%，未够 1.5x 阈值）
Monte Carlo：RoR=0% / 99%CVaR=0.01% / 中位 $10043.94
风控清单 3/5（有条件准入）
对拍 PASS 判据：清算次数 13±3 / 总资产 $11288 ±5% 同量级 / 胜率 ~77%。
（关键：Bank 在此数据零触发，故一期无 Bank 版本应高度接近原版，对拍应很干净）
5.5 ⚠️ 下一步 = Step 2 策略类复核（新 AI 第一件事，已有指令未执行）
Step 2 不能凭 CC 自报清单签 CLEAN（CC 报了 16 移植 + 5 排除的表格，但那是 verdict）。新会话第一件事 = 让 CC dump v25_trend.py 全文，复核方逐行核。
dump 指令（已备，直接转 CC）：
【任务】Step 2 复核 — dump v25_trend.py 全文（纯只读，不改不写不测）
单个 Bash 块只跑 cat -n，原样回贴，禁表格/摘要/"见上方"：
  cat -n src/hermes/strategies/v25_trend.py
若超长分两条：sed -n '1,200p' / sed -n '200,400p'，各独立成消息
回传后 STOP，等复核方 verdict
复核方逐行核清单（V 判据，CLEAN 全绿才放 Step 3）：

V-a 加仓 avg_price 递推 == (size*avg+cost)/(size+sz)（分子分母不能错，移植头号坑）
V-b pos 循环判定顺序 == 止损→止盈→移动止损→金字塔→dominance（顺序错毁对拍）
V-c 三段止损：-0.18 / -0.12&add>0 / -0.06&not ss（半仓 once）
V-d 多级止盈 cs 比例 0.10/0.10/0.15 + cs<size 守卫
V-e 移动止损 me=p<peak-6*atr or dd2>0.55，且 pnl>0 才 return（pnl≤0 不移仓 = V25 原版行为，忠实复制）
V-f 入场 en 五条件全 == 原版
V-g bp 分级 0.025/0.040/0.075 + rm 写死 1.0（不得残留 rm 变量，§6.2 防越界）
V-h Bank/pk/dd/m15/m20/MC 代码级零残留（grep 确认）
V-i quantity 走 Quantity.from_str（Decimal 口径）；指标内部 float 可接受（C 层 + SMACrossover 先例）
V-j 无 look-ahead：high_20/recent_low 用 shift(1) 等价，信号只用 bar.close + 历史

为什么必须逐行核：392 行里藏一个递推错误或漏条件，回测照样跑出「一个 PnL 数字」，错误逻辑碰巧落进 ±5% 就是假过。§6.2「删条目」最隐蔽场景。

6. 关键参考资料 / 数据 / 格式要求（背景）
6.1 移植模板与管线（Phase 0 recon 坐实）

NT 策略模板 = src/hermes/strategies/sma_crossover.py（现成 NT Strategy：Config(StrategyConfig, frozen=True) + class(Strategy)，on_start subscribe_bars，on_bar 信号，order_factory.market 下单，Quantity.from_str）
回测接线模板 = experiments/wp4_sma_backtest/main.py（BacktestEngine：add_venue NETTING/CASH + add_instrument make_solusdt_binance + add_data bars + add_strategy + run + get_result，取 result.total_orders / total_positions / stats_pnls["USDT"]）
OHLCV→Bar = src/hermes/adapters/nautilus/binance.py::kline_to_bar（入参 hermes Kline + bar_type + price_precision + size_precision；要求 kline.is_closed=True；内部 Price/Quantity.from_str(format(d,'f'))，全 Decimal）
NT 1.227：on_bar(self, bar)；Bar 字段 open/high/low/close/volume/ts_event/ts_init；OrderFactory 有 market/limit
成交语义（V2 硬线）：on_bar 收到的是已收盘 bar，信号只用 bar.close + 历史，NT 默认 close 成交 → 无 look-ahead

6.2 数据特性（重要，CC Step 1 发现）
data/SOLUSDT_2024_1h.csv 有两行开头表头（timestamp,... + open_time,...）外加 ~12 处月份边界嵌入表头行（散在 row 746/1443/2188… 等）。加载器靠「int(row[0]) 失败即 continue」过滤全部表头行，与原版 pd.to_numeric(errors='coerce').notna() 等价 → 精确得 8784 根。这是数据本身特性，非 bug。列序（0-indexed）：0 open_time_ms / 1 open / 2 high / 3 low / 4 close / 5 volume / 6 close_time。
6.3 V25 原版逻辑（移植对象，源在 experiments/v25_reference/v25_original.py）
完整源已落服务器（§5.3）。核心：calc_indicators（EMA/ATR/ADX/high_20/recent_low/consecutive_up）+ run_v25（pos 循环维护 + 模块B Bank + 模块C 入场）。一期只移 run_v25 的单 bar 持仓循环 + 模块C 入场，不移模块B。
6.4 用户沟通/工作要求

中文、直接、表格优先、一次一个问题、错误直接承认不软化
严格 checkpoint：每 Step 做完 STOP 等复核，不一口气跑完
raw 优于 verdict；CC 自报清单不充当证据
粘贴易截断：CC 回贴长文常被聊天吞字符（本会话多次），dump 长文件须分段、单文件单块、禁摘要


7. ce9 守卫（改 PREC/RISK 前必查，承 34/35.md）
tests/unit/execution/test_post_only_routing.py::TestNeighbourZeroDrift::test_ce9 对四文件整文件字节 md5 锁死：precision bb0308ce / nt_translate 045be165 / nt_submit db974054 / guard b93da5a2。改/标/动这四个任一字节须先查清 ce9 意图，不可擅改 baseline md5。本会话线 B 全程未碰这四个（只新增 v25_trend.py + experiments）。

8. 其余 backlog（承 35.md，不在当前路径）
自建链 a/b1/b3a DORMANT（真单验稳后议去留）；B5 vacuous（无 telegram）；exponential 同病已排除关闭；LC-沿袭-1/2/3 未处理；LC-XPLAT-1（fcntl Windows）低优先。

9. 新会话接手指引

读本文档全文（不读就自驱推进是禁止行为）；如可检索，并读 30–35.md 补全 E2.5 + M2 全貌
先复验三闸（SS6.7 + SS6.8.1，CC 主体）+ 锚对账（HEAD=f2a86c9 / baseline 692 / 三闸指纹见 §3）；GATE-1 模型见历史 L9
两线优先级：testnet 一旦恢复（ping=200），线 A 真单优先于线 B（M3 收口 > 新开 M2）—— 停下移植，Iris 手敲 §4 那条 trade-live
线 B 下一步 = Step 2 策略类复核（§5.5）：转 dump 指令给 CC → 复核方按 V-a~V-j 逐行核 → CLEAN 才放 Step 3，有问题退回修。禁止凭 CC 自报清单签 CLEAN
改任何 PREC/RISK 文件前查 ce9（§7）；STOP-DOCS / STOP-B / raw 纪律全程在身；commit/push + 真单永远 Iris 手敲
