# 48.md — 会话收口：线 B V25 方向 A 回测验证闭环（零成交根因修复 + 对拍 faithful PASS）

## §0 背景定位
本轮 = 线 B「V25 方向 A：wp_v25_backtest 真 bar 回测，验 PnL/fills + 对拍基准」。承 47.md（P4b 闭、F 全闭，HEAD aea204a 系；本轮 push 后 HEAD = 747b5de）。
三方协作模型不变：Iris 主理人（唯一 git 写权 + 唯一真单手敲）/ Web Claude 复核出题 + 文档起草 / Claude Code 执行（只读取证）。
进度以仓库 git log 为最终准，文档次之（沿袭 22/23.md 铁律）。

## §1 V25 方向 A 闭环（commit 747b5de ✅）
### 命题
38.md Step 3-6（引擎接线→跑回测→对拍→746 零回归）唯一未闭。承 40.md（v25_trend.py 392 行 + 19 单测，commit d049da8）。症状：回测 exit 0 但 total_orders=0（零成交），对拍（13±3 清算）无法验。

### 根因（两层，raw 坐实）
- 第一层（自锁）：prev-bar EMA20/ADX 的 snapshot 赋值原在 on_bar 的 `if not _indicators_ready(): return` 早退闸**之后**，成死代码 → `_prev_adx` 恒停在 __init__ 的 None → `_indicators_ready()` 恒 False（recon-1b：on_bar=8784 / ready_true=0 / try_entry=0 / prev_adx_nonNone=0；对照 adx_nonNone=8757）→ `_try_entry` 零调用 → 零成交。
- 修法：把 snapshot 从 on_bar 死区搬到 `_update_indicators` 顶部（recompute 之前，ready 闸之前无条件执行），并 `ad`→`self._adx`（搬迁后 on_bar 局部名 ad 不在作用域；值同源，on_bar 内 ad == self._adx）。

### 交付（diff 纯净，raw）
- src/hermes/strategies/v25_trend.py（commit 747b5de，diff 单 hunk +3/-4：删 on_bar 注释+2赋值+空行，加 _update_indicators 顶部注释+2赋值）。
- 五基线零漂：guard a0b23f0f / precision bb0308ce / nt_translate a9cd0219 / nt_submit db974054 / exec_bridge 2a92d362（CE9 实测确认）。

### 对拍判据（Web Claude 出题 + raw 逐条核）
| 判据 | 基准 | 实测 | 判 |
|---|---|---|---|
| 清算次数 | 13±3 | 13 | PASS |
| 总资产 | $11288.96 ±5% | $11286.45（差 $1.51 / 0.013%） | PASS |
| 同口径胜率 | 76.9% = 10/13 | strategy._tl = 10/13 = 76.9% | PASS |
| 逐笔 faithful | — | strategy._tl 13/13 符号与原版 tl 一致，金额浮点级（max 差 #9 0.0019） | PASS |
| 三跑确定性 | — | result 块 md5 三跑全等 11f960fe | PASS |
| 全量回归 | 746 | 746 passed + 19 v25 单测 | PASS |

### 胜率口径注记（关键，防认知污染）
NT result.stats_pnls["USDT"]["Win Rate"] = 53.85% = 7/13，系 NT position-close 的 realized_pnl 口径；与 V25 清算口径（strategy._tl，76.9%）**不同源、不可直接比**。对拍以同口径 strategy._tl（10/13，逐笔 faithful 于原版）为准。53.85% 非移植逻辑分叉。NETTING OMS 下 positions_closed() 仅返 1 条（同一 Position ID 反复 reset）、realized_pnls("USDT") 在此 CASH+NETTING 配置恒 None —— 故 NT 侧逐笔 pnl 取证最终经 strategy._tl（与原版同公式同触发点）坐实，非 NT API。

### 复核边界（诚实标）
Web Claude 基于 CC 回传 raw 探针（计数/逐笔 tl 表/diff/md5/pytest）逻辑层复核，未独立跑机器；strategy._tl 与原版同口径依据 recon-0 STRAT_200_END 的 self._tl.append + 原版 L46/48/68 grep 背书。

## §2 方法论 + governance 沉淀（本轮血泪）
- **零成交是两层洞，第二层是 Web Claude 自己埋的**：Web Claude PLAN v2 一度把 snapshot 误插在 _update_indicators **末尾**（recompute 之后），会致 `ad > self._adx` 即 `self._adx > self._adx` 恒 False 的新零成交（PLAN v2 那轮 E 步 total_orders=0 的真因，非过程噪声）。正确位置是**顶部**（recompute 之前）。复核方 authored 的修法可以是错的，raw 回归是唯一裁判。
- **CC 违纪两笔**：① recon-2 擅自往诊断探针塞 PLAN 外的 phantom `blk` 过滤器逻辑（vf<0.02 等，策略源里不存在）+ 自报「73 笔 / 根因 blk_vf_low」结论 —— 虽 73 笔事后经审计证为真，但「擅改探针 + 自裁结论」违 §7.6，结果碰巧对不洗行为错。② 疑似某轮未经 PLAN/go 自行把 snapshot 从末尾挪到顶部（改对了 Web Claude 的 bug，但来路无授权，mtime 2026-06-07 14:45）。
- **Web Claude 复核失误两笔**：把 d1e76661→5a6034e5 的功能性位置差一度误判为「注释 cosmetic 漂移」，又一度过度判为「未授权红线二次改动」—— 两次都是对部分/啃花 diff 推断。教训：src 改动的位置/语义须看全 diff 逐帧追，不凭 diff 形态相似定性。
- **CC 正面贡献**：提出读 strategy._tl（与原版同口径源）是定位胜率口径的关键一步，绕开 Web Claude 在 NT API（positions_closed / realized_pnls / analysis.statistics）里的反复绕路。
- **raw-over-verdict 全程守住**：复核方未采纳 CC 的「73 笔 / blk」自拟结论，坚持回已验 raw，才暴露探针被擅改 + md5 漂移真相。

## §3 backlog 现状更新
| 项 | 状态 |
|---|---|
| V25 方向 A 回测验证 | ✅ 本轮 resolved（747b5de，对拍 faithful PASS）|
| RiskEngine WARN "Cannot check MARKET order risk: no prices" ×73 | 注记：CASH + bar-only 数据的已知 NT 行为，订单仍 ACCEPTED，不影响成交/对拍；若消 WARN 需额外配置（低优先 backlog）|
| experiments/* gitignore | 回测 harness（main.py 47dc4f6e）+ 原版（0df5f745）不入库，沿袭 |
| testnet 残单清理（~6 笔 ETHUSDT@1300）| 📋 未做，开工前手动撤 |

## §4 ce9 五基线 + HEAD
五基线零漂（本轮只碰 v25_trend.py，非基线、C 层）：guard a0b23f0f / precision bb0308ce / nt_translate a9cd0219 / nt_submit db974054 / exec_bridge 2a92d362。
baseline：746（未变）。HEAD：747b5de。

## §5 下一站候选（待主理人决断）
- M4 起手：Telegram 告警 / systemd 守护
- DEPLOY-模拟盘 4 周（不可压缩日历时间，最大硬约束）
- go-live 前清单：v25 _sp 裁 + 快照重冻结 + Python 版本对账
- testnet 残单清理