35.md -- 会话交接：b3b-live 发单入口 impl 闭（代码入库，真单未打）+ 第二步真单待触发（2026-06-03）

承接：docs/handoffs/34.md（backlog 1 + 自建链标记 + §6 收口，锚 aec6bd8）。本文档为干净收口交接，锚 16eedd0。下一会话第一件事 = b3b-live 第二步真单（三件套已备，见 §4）。

0. 背景定位
项目：hermes —— 基于 NautilusTrader（NT）的 Binance 自动交易系统；当前在 E2.5-b3b-live 轨（发单入口已建，待打第一笔 testnet 真单）
承接自：34.md（锚 aec6bd8；工作树 clean）
本会话已做（均落库）：
① GATE-3 发单闸重验 CLEAN（c9c4cbb0、socket-pre PreToolUse 拦截、触发 deny 无连接握手）
② b3b-live 发单入口开发：recon×2（cli 缺口 + NT 喂单机制）→ 链 B PLAN → CC implement → D1–D5 mock 验证 → 复核 CLEAN → commit（16eedd0）
③ 链 A/B 决策：选链 B（经 NT factory，验真路径），否链 A（验错对象）/ 否 P2 绕闸（红线）
当前态 = 干净收口，工作树 clean，发单入口代码入库。但 b3b-live 第一笔真单【尚未打】——代码 commit ≠ 真单已发（L13）。下一会话第一件事 = 第二步真单（§4）

1. 红线（承 CLAUDE.md，不变）
绝不 git add/commit/push（CC 被 GATE-1 物理拦，主理人手敲）
STOP-DOCS：预览先行 + 预览 md5 == 写入 md5；写后等显式 go 才 cp（L1）；落盘须 CC cp 原样复制 + md5 比对，勿经编辑器（L12）
STOP-B：连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐

2. 角色与流程约定（关键背景，新 AI 必读）
三方：CC（Claude Code，本机跑文件编辑/测试；git-write 被 GATE-1 物理拦）/ Iris = 主理人 = 用户（手敲 git commit/push + 手敲真单 trade-live，唯一执行主体）/ 复核方（本对话角色：出 PLAN/判据、复核 raw、签 CLEAN，不碰执行）
节奏：recon → PLAN（复核方）→ implement（CC）→ 验证 raw（CC）→ 复核签 CLEAN → Iris 手敲 commit
raw 纪律：CC/Iris 的「done / CLEAN」是 verdict，git 读 / 测试输出是 raw，raw 翻 verdict（L7/L8）。复核方对存在性/状态类断言一律以 raw 为准、不取转述
发单纪律：真单只由 Iris 手敲 trade-live（去 --dry），TRADE-GATE 物理拦 CC（SS6.8.1）；复核方不替起头、不驱动 CC 发单、绝不找闸字面串漏洞绕过（P2 已否，红线）

3. 当前基线快照
git 锚：origin/main = 16eedd0；链 6ec1e2a → ce3204f → aec6bd8 → 85fd1da → 701323c → 16eedd0
baseline：692 passed（承 34.md 649 + 本会话净增 43；构成 +51 新 [29 strategy + 22 cli] − 8 wiring 迁移）；ce9 仍 passed
三闸指纹（承 34.md §3，本会话未变）：git-write 281c070d / mainnet 02ed4b83 / trade c9c4cbb0 / settings 625c1a96
三闸行为：GATE-1 本会话验活（deny、HEAD 未动）；GATE-3 本会话重验 CLEAN（c9c4cbb0、socket-pre 拦截、触发 deny 无连接）；GATE-2 未触碰

4. b3b-live 第二步真单（最高优先，下一会话第一件事）
状态：三件套【全齐】，差的只是 Iris 手敲那条命令。
  ① 发单闸 CLEAN —— ✅ 本会话 GATE-3 重验（c9c4cbb0、socket-pre）
  ② Iris 显式 STOP-B —— ✅ 本会话已给：授权 b3b-live 去 --dry、真起 testnet TradingNode、连 Binance testnet、打第一笔真单
  ③ 复核方发单前判据 J1–J6 —— ✅ 本会话已出（下录）
真单参数（STOP-B 锁定，逐字不得偏离）：
  symbol=ETHUSDT / side=BUY / type=LIMIT / price=1300 / quantity=0.0039 / environment=TESTNET
  自洽校验：1300 × 0.0039 = 5.07 USDT > minNotional 5 ✅；1300 远低于现价（查时 1854.64）→ 限价买单不立即成交，验"单挂进去"而非真成交 ✅；testnet 余额 ETH 1.0 / USDT 9974.74 够
执行（Iris 手敲，去 --dry）：
  hermes trade-live --symbol ETHUSDT --side BUY --price 1300 --qty 0.0039
建议时序：下轮先【干跑校验】（--dry 确认接线 / 或真起 node 但 strategy 不 submit，验 J1 testnet 连接 + J2 五字段打印对）再真发；真单不可逆（Category B），踩稳再上
J1–J6 判据（真打单逐条满足，任一不满足即 STOP）：
  J1 发单前置：node environment=TESTNET 显式、base=testnet.binance.vision；出现 mainnet 域名（api.binance.com）立即 STOP；credentials 走 env
  J2 submit 前校验：order 五字段 raw 逐字 == 参数；过 precision round 后仍 = 1300/0.0039（未走样），差一字 STOP
  J3 执行边界：只提交一笔，max_retries=0，不重试/追单/撤他单
  J4 打单后坐实（必回四样）：① orderId ② status（预期 NEW=挂单未成交；若 FILLED/PARTIAL → 意外成交 STOP）③ 下单回执全文（price/origQty == 1300/0.0039）④ node testnet 连接日志
  J5 异常中止：拒单码（-1013/-2010/-1021/-1100 等）→ STOP 贴 raw，不重试/不调参再发（改参须重走 STOP-B）；socket 失败 → STOP
  J6 收尾：发单无代码改动不 commit；打完 git status 须 clean

5. 链 B 实现细节（16eedd0）
路径：node = TradingNode(config, env=TESTNET) → add_exec_client_factory("BINANCE", BinanceLiveExecClientFactory) → node.build() → node.trader.add_strategy(OneShotStrategy) → node.run()
OneShotStrategy（src/hermes/execution/oneshot_strategy.py）：
  on_start：order_factory.limit(instrument_id, side, qty, price) → submit_order 一次（_submitted flag 防重）
  on_order_accepted：捕 venue_order_id（= Binance orderId）→ on_done(venue_order_id, None) → node 停
  on_order_rejected：捕 reason → on_done(None, reason)
  OneShotConfig(StrategyConfig, frozen=True)：instrument_id_str / side / price_str / quantity_str
cli.py：trade-live 加 --symbol/--side/--price/--qty；非 --dry 时 _parse_instrument_limits（从 exchangeInfo filters 取 tick/step/notional 构造 InstrumentLimits）→ precision round → 构造 strategy → build/add_strategy/run；--dry 仍 exit before node.run()（零网络）
testnet 显式硬编、max_retries=0
ce9 四文件零改动（precision 仅 import 调 round_price/round_quantity，未改字节）

6. 链 A/B 决策记录（为何 B、为何否 A/P2）
链 B（经 NT exec client，选用）：node.build 后 trader.add_strategy → NT 内部路由 → binance_exec_client._submit_order → REST。验真 NT factory 路径（b3b-live 本意），产 J4④ 连接日志
链 A（直 REST 绕 NT，否）：order_spec → nt_submit → binance_rest.new_order → REST。工作量最小但验错对象（验自建链非 NT factory），且把刚标 dormant 的自建链拉回 live，与挂起决定相悖
链 A 包 build 后（否）：语义混（NT 连接 + 自建链发单），J4④ 名实不符
P2 Python 直跑绕 CLI（否，红线）：写脚本绕开 TRADE-GATE 的 hermes trade-live 字面串让 CC 发单 = 蓄意规避发单闸，授权打单 ≠ 授权 CC 绕闸代发
NT 1.227.0 关键事实：无 TradingNode 层外部 submit（B-ii 不存在）；唯 Strategy 路径（B-i）可行

7. 剩余 backlog（承 34.md §6）
1. B5 vacuous：无 telegram 模块，Telegram 建成后重验 SS6.8.2(3) —— 不动
2. CLI 层 trade-live 真执行测：本会话已补（test_cli.py 22 测，mock 验发单入口）；真单实跑验证 = 第二步（§4）
3. 自建链 a/b1/b3a dormant：34.md 已标 DORMANT（aec6bd8），挂起，b3b-live testnet 验稳后重议去留。注意 nt_translate/nt_submit 受 ce9 字节冻结，动前查守卫
4. exponential 同病：34.md 查清关闭（裕度比 ~0.12，不同病）

8. ce9 守卫（承 34.md §5，改 PREC/RISK 前必查）
位置 tests/unit/execution/test_post_only_routing.py::TestNeighbourZeroDrift::test_ce9，对四文件整文件字节 md5 锁死：
  precision.py bb0308ce6cf0b50f5214e5d9819f2cc8 / nt_translate.py 045be1651ba9b0f0561d66c8f3a10cd5
  nt_submit.py db974054f08f4a15ee3357cd74a565cb / guard.py b93da5a226ac8a59f336dab525c99a24
铁律：改/标/动这四个任一字节须先查清 ce9 意图（docstring 无 why），不可擅改其 baseline md5

9. 关键教训（承 30–34.md L1–L11，本会话新增 L12–L14）
L1 STOP-DOCS：预览 md5 == 写入 md5、等显式 go 才 cp
L6：GATE 行为自测主体必 CC，须核 prompt 来源（落 ● Bash 工具块、非 Iris shell）
L7：见「闸该拦没拦」先分清执行主体，勿把 Iris-shell 假绿读成红线事故
L8：假绿 commit —— nothing to commit / Everything up-to-date 是「add 错文件」红旗；「done」须四样 raw 坐实（diff --cached --stat 非空且 scope 匹配 → [main <sha>] → push <old>..<new>）。本会话 ce3204f/aec6bd8/85fd1da/701323c/16eedd0 五 commit 均坐实
L9：GATE-1 = PreToolUse 预检 string-match；读类命令吐 footer 不触 deny，勿误读为 bypass
L10：dormant ≠ 可改，文件可能被 md5/snapshot 字节冻结（ce9）；标记/删除前查守卫（grep snapshot/md5/baseline/read_bytes），不只查 import
L11：commit msg 含括号/斜杠须 git commit -m "..." 带引号；裸贴入终端被 shell 解析炸（syntax error）
L12（新）：STOP-DOCS 落盘须 CC cp 原样复制 + 落盘后 md5sum 比对 == 预览基准，一致才 commit，勿经编辑器（34.md 经 VSCode 落盘吃了末尾换行，md5 漂 f551ba67→3b85d27d，无害但流程破）
L13（新）：发单入口代码 ≠ 真单已打。代码 commit 入库只是"能发单的代码就绪"；真单是独立第二步、Iris 手敲 trade-live 去 --dry、走 J1–J6。勿把 16eedd0 读成"第一笔真单已发"
L14（新）：发单参数自洽校验，minNotional 按【挂单价 × 数量】算（非现价）。本会话两次踩坑：0.001（现价算 1.85<5）、0.0027（挂 1300 算 3.51<5）；最终 0.0039（1300×=5.07>5）。改价/量任一须重算名义值过线

10. 新会话接手指引
1. 读本文档全文（不读就自驱推进是禁止行为）；如可检索，并读 30–34.md 补全 E2.5 全貌
2. 先复验三闸：SS6.7 + SS6.8.1（CC 主体），指纹对账以 §3 为准（git-write 281c070d / mainnet 02ed4b83 / trade c9c4cbb0 / settings 625c1a96）；GATE-1 模型见 L9
3. 锚对账：git log 须 HEAD = 16eedd0，链见 §3，工作树 clean；baseline 692
4. 【下一会话第一件事】= b3b-live 第二步真单（§4）：三件套已备，建议先干跑校验再 Iris 手敲 trade-live 真发，走 J1–J6，打完贴 J4 四样 raw 复核
5. 改任何 PREC/RISK 文件前查 ce9（§8 四文件）；STOP-DOCS / STOP-B / L6 / L8 / L10–L14 全程在身；commit/push + 真单永远 Iris 手敲