# 51.md — 会话收口：M4-刀2-a（binance_rest signed-read 三端点 + test_config 隔离修 / ruff-mypy 净引入零的三轮 raw 拉锯）

## §0 背景定位
本轮 = backlog「M4-刀2：账户类告警」的第一刀 = 刀2-a（signed-read infra）。承 50.md（HEAD c9cb6e8，baseline 789 实测，M4-刀1 活体闭）。
本轮 push 后 HEAD = <2a-SHA>。
三方协作模型不变：Iris 主理人（唯一 git 写权）/ Web Claude 复核出题 + 文档起草 / Claude Code 执行（只读取证）。
进度以仓库 git log 为最终准，文档次之（沿袭 22/23.md 铁律）。

## §1 刀2-a 闭环（commit <2a-SHA> ✅）
### 命题与拆刀
M4-刀2 = 账户类告警（余额/成交/撤单）。recon（50.md）证实 binance_rest 缺 account/orders/trades 只读端点 ->
刀2 拆两段：刀2-a = 给 binance_rest 补三个 signed-read GET 端点（本轮）；刀2-b = 告警逻辑叠加（留独立轮）。

### Phase 0 recon（六轮 raw 拉锯才拿到 sentinel 逐字）
- R0-1~7 + 基线对照：binance_rest 唯一 signed = new_order(POST)；_request 第 3 形参 method:str 完全参数化（docstring 明列 GET/POST/PUT/DELETE）-> signed GET 是纯加法，_request 零改动。
- sign(query_string, secret) method-agnostic（对 query 串签，与 GET/POST 无关）-> 三端点直接复用，零重造 crypto。
- BinanceCredentials 在 binance_credentials.py:33，非 ce9 五基线，client 直持 _api_key/_api_secret str 字段。
- 关键过程教训：CC 连续多轮回「汇总」而非 raw（「GET×4」「signed:bool=False」是消化结论），复核方据 §6.2 拒签；最终靠 Iris 终端直跑 sentinel（<<<R0-X / R0-X>>>）+ 捕获 /tmp + cat，绕开「CC 汇总层 + UI 折叠」双重问题，才拿到 132 行逐字 raw（md5 ce1e794e）。

### 交付（STOP-ARCH 三文件域）
- src/hermes/exchanges/binance_rest.py（+85，三 async GET 方法插在 __aexit__ 后、_request 前）：
  get_account（GET /api/v3/account，无 symbol）/ get_open_orders（openOrders，symbol 选填 .upper()）/
  get_my_trades（myTrades，symbol 必填 + limit 1-1000 校验 + startTime/endTime/fromId 选填）。
  全走 _request("GET",…,signed=True)；_request/__init__/new_order 一字未改。
- tests/unit/exchanges/test_binance_rest.py（+280，mock httpx，72 passed）。
- tests/unit/monitoring/test_config.py（L27 一行修，见 §2）。

### 判据 AR1-7 + FX-iso 总判（Web Claude 出题 + raw 逐条核，非 CC verdict）
AR-1 签名复用（三方法零自造 crypto，sign/hmac 命中仅旧址）PASS
AR-2 凭证零回显（token-pattern=0）PASS
AR-3 base_url 不硬编码（host 字面量仅 BinanceEnvironment 配置表，新方法走 _make_url）PASS
AR-4 三端点参数正确（逐行核 path/signed/必填性）PASS
AR-5 零交易面 / GATE-3 不动（method 恒 GET，new_order 未改）PASS
AR-6 mock 单测（新文件 72 passed，零真网络）PASS
AR-7 五基线零漂 + git scope 三文件 + ruff/mypy 净引入 0 PASS
FX-iso test_config L27 隔离修（全量 789 passed 全绿）PASS

### 复核边界（诚实标）
Web Claude 基于 CC re-emit raw + Iris 终端直出 sentinel 逐字，逻辑层 + 源码逐行 + 新旧基线（git show HEAD: 导 /tmp）逐条对照复核，非独立跑机器。

## §2 test_config.py L27 隔离修（刀1 埋的雷，本轮踩响）
- 现象：刀2-a 跑全量回归出 1 red：test_missing_token_raises_validation_error（monitoring，刀2-a 未触）。
- 根因（C2 raw 实锤）：该测已 patch.dict(clear=True) 清 os.environ，但 TelegramConfig 的 env_file=".env" 让 pydantic-settings 仍读磁盘 .env -> 刀1 活体验证时 Iris 往 .env 加的真 token 被捡到 -> 「缺 token 应报错」不成立 -> DID NOT RAISE。
- 即：刀1 的 .env token 踩响了一条 49.md 写测试时就埋下的隐患（L27 裸 TelegramConfig() 漏了 _env_file=None，而 L39/L53/L66 都有）。
- 修法：L27 补 _env_file=None # type: ignore[call-arg]，跟齐同文件既有范式 + test_credentials.py 的隔离策略。config.py 本体不改（env_file=".env" 是生产正确行为）。
- 判别：物理上「给 binance_rest 加 3 个纯加法方法」不可能让 monitoring 失败（V1 diff 不含 monitoring）-> 非刀2-a 回归，是 monitoring 既有隔离缺陷被新 .env 状态暴露。

## §3 方法论沉淀（本轮新）
- 「净引入」必须新旧基线逐条对照，不是计数相同：CC 报 ruff/mypy「36 条全既有」，但「计数相同」不等于「逐条相同」（行号可能因新代码插入漂移但命中点不同）。复核方坚持 git show HEAD: 导旧版跑同样 lint，逐条比 Δ —— 实测 Δ∈{0,+85}（+85 = 新代码块插入量）、类型/顺序逐一对应、无第三种 Δ、无新增/消失条目，才坐实净引入 0。CC 提出 git show 导 /tmp 对照（替代复核方给的 git stash）更干净——只读 git、零 staged 扰动，正面贡献。
- mypy 门禁口径错配要当场揪正：复核方一度让 CC 跑 mypy test_文件，跑出 87 条 no-untyped-def；CC 核对 pyproject [tool.mypy] files=["src/hermes"] 指出 test 文件从不在 mypy 门禁内、这 87 条是口径外探针不计入 AR-7。CC 这条澄清正确，挡掉了「新增 87 条 mypy 违规」的误判。出题方设的检查口径也会错，执行方读 live 配置纠偏是后挡。
- raw 胜口头判断（secret 场景 + verdict 场景双现）：① 50.md token 泄漏轮，Iris 口头判「没写进 .env.example」，CC git diff raw 打出真值 -> 按已泄漏处置（轮换）。② 本轮 staged 状态，复核方口头判「你已 git add（staged）」，CC porcelain raw 显示 ` M`（首列空格=未 staged）-> 据 raw 纠偏，commit 前需重 add。两次都是 raw 推翻口头前提，承铁律。
- 既有债搁置不混入本刀：binance_rest/test 既有 28 条 ruff/mypy 债（UP037/F841/B904/W292/I001/E402/F401/W291 + type-arg 等）经逐条对照确认 HEAD 既有、刀2-a 零引入零放大 -> 开 backlog 搁置，不在「绿地加法」刀里顺手改（承刀1「既存债不碰、原样保留」）。
- 终端啃花 + CC 汇总层双重阻断的对策固化：sentinel（<<<X / X>>>）+ 捕获 /tmp + cat + Iris 终端直出（绕 CC 汇总）+ md5/wc 锚。本轮 recon 拉锯多轮的核心症结就是 CC 反复汇总 + UI 折叠 stdout，最终靠 Iris 直跑终端解。

## §4 ce9 五基线 + HEAD
五基线零漂（本轮碰 exchanges 非基线层，零触五基线）：
guard a0b23f0f / precision bb0308ce / nt_translate a9cd0219 / nt_submit db974054 / exec_bridge 2a92d362。
baseline：789（748→…→789；本轮 +0 净，FX-iso 把 1 red 修回绿、刀2-a 测试已含在 789 内）。HEAD：c9cb6e8 -> <2a-SHA>。

注：baseline 计数沿革——49.md 记 774；刀1 活体轮（50.md）无代码；刀2-a 实现引入测试后全量为「788 passed 1 failed」，FX-iso 修 L27 隔离后转 789 passed 全绿。789 = 刀2-a 测试 + L27 修复后的全绿态。

本轮交付物（commit <2a-SHA>，binance_rest 非基线、C 层）：
src/hermes/exchanges/binance_rest.py（+85）、tests/unit/exchanges/test_binance_rest.py（+280）、tests/unit/monitoring/test_config.py（L27 1 行）。

## §5 backlog 现状更新
| 项 | 状态 |
|---|---|
| M4-刀2-a：binance_rest signed-read 三端点 | ✅ 本轮 resolved（<2a-SHA>）|
| M4-刀2-b：账户告警逻辑（余额/成交/撤单阈值 -> 推送）| 📋 叠在 2-a signed-read 上，独立轮；告警活体验证另走 STOP-LIVE-MON 式 |
| binance_rest/test ruff-mypy 既有债（28 条）| 📋 HEAD 既有、刀2-a 零引入；独立清债轮（或碰 binance 时顺手）|
| M4-刀3：systemd 守护 | 📋 STOP-SYS SS4.6，改 node.run 补优雅停 + 持久化配置 |
| 刀2-a 三端点活体验证（真 testnet signed-read）| 📋 需 .env 真 testnet signed key（Iris 手敲），单测 CLEAN 已入库、活体单独走 STOP-LIVE |
| 快照重冻结 / Python 版本对账 / v25 _sp 裁 | 📋 go-live 前清单（沿袭 47/48/49/50）|
| testnet 残单清理（~6 笔 ETHUSDT@1300）| 📋 开工前手动撤 |

## §6 下一站候选（待主理人决断）
- M4-刀2-b（账户告警逻辑，叠 2-a signed-read）
- 刀2-a 三端点活体验证（STOP-LIVE，真 testnet signed key）
- M4-刀3（systemd 守护，STOP-SYS）
- binance ruff/mypy 既有债清理（独立轮）
- DEPLOY-模拟盘 4 周（不可压缩日历时间，最大硬约束）