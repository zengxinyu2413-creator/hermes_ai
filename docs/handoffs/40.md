# 40.md — 会话收口：线 B M2 V25 — V25TrendStrategy 移植 + 单测落库

## 0. 背景定位

本会话即 39.md §7② / §9 标的「线 B M2 V25 复核（需单独会话）」。承 39.md C4（线 A b3b-live 已收口于 `e066570`）。本线 scope：仅 V25TrendStrategy 本体忠实性核查 + 补 B1 纯逻辑单测，不碰线 A，不动 ce9 四文件。

## 1. 红线（承 CLAUDE.md / 39.md，本会话无修订）

- 双轨制（§6.4）：判据 / 验收框架由复核方出，CC 执行达标，CC 不自判 CLEAN。
- git 写操作（add / commit / push / handoff）全程手敲，CC 物理不 commit。
- 不碰 ce9 四文件：guard.py / precision.py / nt_translate.py / nt_submit.py。
- 发单只走 testnet（承 39.md §1）。
- 纯只读核查阶段：不写、不发单、不外网、不动 docs / CLAUDE.md。

## 2. 角色 / 流程 / 三闸指纹（承前）

承 39.md §2，无修订。本会话流程：PHASE0 只读预查 → 复核方裁 mock 方案（M2）→ PHASE1 PLAN → CC 执行写测 → 六项 raw 验证 → 复核方签 CLEAN → PHASE2 手敲 commit。

## 3. 基线快照

- 入会基线（39.md）：**692 passed**。
- 出会基线（本轮）：**711 passed**（+19，纯新增 V25 单测，既有 692 零回归）。
- CLAUDE.md 手册旧锚 590 已过期 121（跨会话累积，非异常）；建议下次顺手校正（见 §7④）。
- HEAD：`e066570`（入）→ `d049da8`（出），已 push，`origin/main` 同步。

## 4. 核心成果 — V 系列忠实性核查 + B1/M2 单测

### 4.1 V 系列裁定（字面托底，raw 出自 sed / grep 原样）

对照源：`experiments/v25_reference/v25_original.py`；港件：`src/hermes/strategies/v25_trend.py`。

- **V-b 硬止损执行体**：−18%（L240）、−12% post-pyramid（L249，`add_count>0` 门）两级均 `_eq += size*p` → `close_all_positions` → `return`，无 fall-through。忠实。
- **V-c soft-stop ss 守卫**：−6%（L258）halve once，`po["ss"]` 置 True 幂等，不 return（原版同）。忠实。
- **V-d TP 三级阶梯**（L262–294 对照原版 L50–62）：阈值 0.08 / 0.15 / 0.30、卖出系数 0.10 / 0.10 / 0.15（第三级独大）、每级 `if cs < po["size"]` 守卫 —— 三项逐级字面复刻，缺一不可，全过。
- **V-e ATR/dd2 trailing stop**（L294–330）：`me = p < peak − 6×ATR or dd2 > 0.55`；仅 `tp>0 且 pnl>0` 才 close；`pnl<=0` 为 L307 注释标的「V25 faithful」边缘态（记 equity、`_tl.append`、但不平不 return，落入 pyramid）。原样保留，不当 bug 修。
- **V-g 账户级资管移除裁定 = A（CLEAN，有意移植）**：成体系移除 rm 回撤保护（原版 L86）+ bank 落袋（L87–95）+ m15/m20 里程碑，非零散抄漏；L4 / L372 注释明示单周期 scope 决策；账户级风控延 RISK 层（ce9）承接，分层合理。
- **V-h 旧符号残留 = 零**：grep 仅两处文档性命中（L4 模块注释、L372 行内注释），无任何半死的 bank / dd / m15 / rm 代码引用。V-g=A 物理前提成立。

### 4.2 单测：M2 路线 19 case

- 路线 B1（纯逻辑，mock NT 依赖），mock 方案 M2：`_TestableV25` 子类覆写 `_buy` / `_sell` / `close_all_positions` 为计数器，`__new__` + 手填状态绕过 Cython `Strategy.__init__`。
- 四测试集：
  - S3-A 硬止损（5）：−18%/−12% close + return + 无 fall-through；−12% 须 `add_count>0` 门。
  - S3-B ss 守卫（3）：−6% halve once、ss=True 后幂等、soft-stop 不 return（落入 TP）。
  - S3-C TP 阶梯（6）：tp1@8% / tp2@15% / tp3@30% 各级量核对；`cs>=size` 守卫 case（不卖、tp 仍推进）；tp 状态机 0→1→2→3。
  - S3-D trailing（5）：D1 ATR / D2 dd2 / D3 faithful 边缘（me=True、tp>0、pnl<=0 → 不 close、不 return、eq 增 + append）/ D4 tp=0 门 / D5 mp<=0.10 dd2 失算。D3、D5 是 B1 路线独有、整函数集成测覆盖不到的边缘分支。
- `TestableV25` → `_TestableV25` 改名，消 pytest 误收集 warning。
- 结果：**19 passed**，既有 **692 passed 零回归**，合 **711**。

### 4.3 一处清理（PHASE1 内，未影响判据）

S3-D D5 原写两行 `at` 赋值（`at=0.001` 死行被后一行 `at=(peak−p)/6.0+0.01` 覆盖）。经 grep 定位实际原文（非凭对话行号推断）后 str_replace 删死行；删后 D5 仍由 `+0.01` 行驱动，单文件 19 passed、全量 711 不变。最终测试文件 402 行。

## 5. 验证（最终 raw 锚点）

- `SUITE_RAW = 711 passed, 13 warnings`
- `V25_MD5 = 649b9588e91c598c223bec39b09c9faf  src/hermes/strategies/v25_trend.py`（核查期间本体零改留底）
- 工作树：核查 / 写测全程仅两文件 `??`，ce9 四文件零出现。
- commit 后 `git log -1 --stat`：`2 files changed, 794 insertions(+)`（392 + 402）。
- push：`e066570..d049da8  main -> main`，`## main...origin/main`（已同步）。

## 6. 本次提交清单

commit `d049da8` — `feat(v25): land V25TrendStrategy with unit tests`

- 新增 `src/hermes/strategies/v25_trend.py`（392 行）
- 新增 `tests/unit/strategies/test_v25_trend.py`（402 行）
- 共 794 insertions，2 files。

message 附 body：单周期 scope（账户级 rm / bank / milestone 排除，延 RISK 层）；19 纯逻辑单测（M2 route）覆盖硬止损、soft-stop ss 守卫、TP 阶梯、ATR/dd2 trailing stop；suite 711 passed。

## 7. 遗留项

① **testnet 残单清理**（承 39.md §7① 滚入，本线未动）：约 6 笔 ETHUSDT BUY@1300.00 GTC 留在 testnet order book，含 S5 venue_order_id=465632。无资金风险（testnet），建议下次开工前手动从 Binance testnet 界面或 REST 撤销。属线 A 尾巴。

② **线 B M2 V25 复核 —— 本轮闭合**：39.md §7② 标的已于本会话完成（V-a…V-h 全核 + 19 单测落库 + `d049da8` push）。线 B 此项关闭。

③ **HERMES_SKIP_INJECT 诊断开关**（承 39.md §7③）：注入块以 `if os.getenv("HERMES_SKIP_INJECT") != "1":` 包裹，保留为诊断入口，将来可议是否清理。

④ **CLAUDE.md 手册基线锚校正**：旧锚 590 落后真实 711 计 121，建议下次顺手更新（非紧急）。

## 8. ce9 守卫

本会话全程零触碰 ce9 四文件（guard.py / precision.py / nt_translate.py / nt_submit.py）；`V25_MD5` + 工作树 `git status --porcelain` 双重留底为证。账户级风控（rm / bank / milestone）按 V-g=A 裁定明确延 RISK 层，strategy 层只做单周期信号。

## 9. 下一会话指引

- testnet 残单清理（§7①，线 A 尾巴，开工前手动撤销）。
- 线 B 后续：V25 方向 A —— `wp_v25_backtest`（NT BacktestEngine 真 bar 喂入，验 PnL / fills），与本轮 B1 单测零重叠，可作下一任务包。
- CLAUDE.md §4 红线 STOP-* 体系完整重建（backlog：CLAUDE-REBUILD-FULL，承 39.md §9）。
- 手册基线锚 590→711 校正（§7④）。