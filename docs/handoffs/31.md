# 31.md -- 会话收口:E2.5-b1 翻译函数 + b2 CLI 骨架 + 三闸行为复验（2026-06-02）

承接:docs/handoffs/30.md（E2.5-a new_order + SS6.6 md5 绑定,，锚 533b1c2）。本文档为本会话收口,，供下个会话无缝接手。

## 0. 背景定位
- 承接自:30.md（E2.5-a 收口,，锚 533b1c2）
- 本会话产出:① E2.5-b1 nt_order_to_new_order_kwargs + 24 mock 测（commit bd75c77）;② E2.5-b2 hermes trade-live CLI 骨架（commit d1ab52b）;③ GATE-1/GATE-3 行为级复验 CLEAN

## 1. 红线（承 CLAUDE.md,，不变）
- 绝不 git add/commit/push（CC 被 GATE-1 物理拦,，主理人手敲）
- STOP-DOCS:预览先行 + 预览 md5 == 写入 md5;写后等显式 go 才 cp（签 PHASE1 != go,，L1）
- STOP-B:连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐

## 2. 本会话核心产出
### E2.5-b1:nt_order_to_new_order_kwargs（bd75c77）
- 新建 src/hermes/execution/nt_order_translate.py:NT Order -> new_order kwargs dict,，反向于 nt_submit.py 的 order_spec_to_submit
- 映射:symbol=instrument_id.symbol.value;side=order.side.name 白名单{BUY,SELL};order_type=order.order_type.name 白名单{MARKET,LIMIT};quantity=Decimal(str());LIMIT 追加 price=Decimal(str()) + time_in_force.name 白名单{GTC,IOC,FOK};MARKET 不写 price/tif 键
- 非白名单一律 raise ValueError,，无静默 fallback
- 测试:24 用例,，happy-path 用真 OrderFactory,，非法 enum 值用 SimpleNamespace（factory 造不出 NO_ORDER_SIDE/STOP_LIMIT/GTD）;roundtrip 喂 new_order 校验段证产物合法
- 验收:全 unit 625 passed（601 + 24,，零回归）;ruff/mypy delta=0
### E2.5-b2:hermes trade-live CLI 骨架（d1ab52b）
- 新建 src/hermes/cli.py:Click group + trade-live 子命令存根,，函数体唯一语句 raise NotImplementedError("trade-live execution not yet implemented (b3)")
- 零外部副作用:无 import 交易所 client、无 TradingNode、无 socket/网络/asyncio
- pyproject [project.scripts] hermes = "hermes.cli:main" 已存在;cli.py 建好后 hermes 不再 crash
- GATE-3 转换:此前靠 hermes crash（cli.py 缺失）兜底;现 crash 消失,，TRADE-GATE 成唯一防线并验活（非-crash 拦截,，承 29.md 待验点闭合）
- 验收:625 passed 零回归;ruff/mypy delta=0;CLI 层暂无单测（见 backlog）

## 3. 当前基线快照
- git 锚:origin/main = d1ab52b;链 d1ab52b -> bd75c77 -> 916a820 -> 533b1c2 -> 9a562f3 -> ed4bc76;工作树 clean
- 916a820:无意义 GATE-1 自测空 commit（误落 Iris shell,，已 push,，决定留,，备查）
- baseline:625 passed
- 三闸指纹（用户级 ~/.claude/,，环境重建须重施）:git-write-gate 281c070d/mainnet-gate 02ed4b83/trade-gate c9c4cbb0/settings 625c1a96
- 三闸行为:GATE-1 本会话 CC 工具块两次 deny 验活;GATE-3 非-crash TRADE-GATE 拦截验活;GATE-2 本会话未触碰

## 4. 关键教训
- L6:GATE 行为自测执行主体必 CC,，raw 须确认命令落在 CC 工具块（圆点 Bash）而非 Iris shell prompt（xinyu@... 美元）。Iris shell 对闸必假绿（L4）;落 Iris 终端的自测既不能证闸坏也不能证闸活。复核方发验闸指令须显式标主体并核 prompt 来源
- L7:见"闸该拦没拦"先分清执行主体再定性,，勿把 Iris-shell 假绿读成红线事故。本会话复核方曾误判一次（把 Iris shell 空 commit 成功读成 GATE-1 失效）,，经 raw 诊断澄清并撤回

## 5. backlog（条件未齐,，不动）
1. flaky test_backoff_capped_at_60s（test_binance_ws.py::TestReconnect）:计时依赖,，须 quarantine（承 30.md）
2. B5 vacuous:无 telegram 模块,，Telegram 建成后重验 SS6.8.2(3)（承 30.md）
3. CLI 层无单测:cli.py trade-live stub 仅 raise NotImplementedError,，b3 接执行时补 CLI 层测试

## 6. E2.5-b 进度 + b3 起手前置（下一步,，未授权）
- a 完成（ed4bc76）/ b1 完成（bd75c77）/ b2 完成（d1ab52b）
- b3 = 真起 testnet TradingNode + exec client 连 Binance testnet + 打第一笔 testnet 真单（Category B,，最高门槛）
- b3 三件套:① 发单闸 CLEAN（GATE-3 非-crash 已验,，但 b3 起 node 时须重验"exec client 实例化/socket 打开前被 TRADE-GATE 拦"路径）② Iris 显式 STOP-B 授权（未给）③ 复核方判据（未出,，待 b3 recon）-- 缺二
- b3 wiring 关联 nt_submit.py 的 LC-PREC-b-1（SubmitOrder 路由到 exec engine）

## 7. 下个会话接手指引
1. 先复验三闸:SS6.7 + SS6.8.1（CC 主体）,，确认 GATE-1/2/3 仍活;指纹对账以第 3 节为准
2. b3 起手前必须:Iris 显式 STOP-B + b3 Phase 0 recon（NT LiveExecutionClient _submit_order/_connect/_disconnect 怎么接 + exec client 怎么拿 BinanceRestClient 实例 + 怎么经 nt_order_translate 调 new_order）-> b3 PLAN -> Iris go
3. STOP-DOCS cp 前必等显式 go（L1）;GATE 行为自测主体必 CC 且核 prompt 来源（L6）
4. 不读本文档就自驱推进是禁止行为
