# hermes_ai 进度交接文档 — RISK 第一刀闭环后 → 下一站 EXEC

> **本文档面向接手的 AI(网页 Claude 或 Claude Code)。** 读完即可无缝接手,无需追问历史。
> 进度基准 = 项目落档的 `docs/handoffs/21.md`(**当前 578 行,以 `md5sum` 实测为准**;前一稳态 md5 `9fb578329cb5b96efb9517b706bc5d5f`,但任何接手动作前都应自验)。
> 用户另有 MasterTracker v3 总规划,**仅供战略参考,不作进度基准**(见 §5)。
> **本会话状态一句话**:M3 RISK 第一刀(killswitch + 连亏熔断 + check_order)全闭环——`src/hermes/risk/guard.py` 交付,14 专测全绿,baseline 409 passed 零回归,CR1~CR8 全 PASS,§2.11 已落档。**下一站是 EXEC(状态机/对账/幂等),尚未开始。**

---

## 0. 接手者必读:角色与第一动作

本项目采用**双轨制**(CLAUDE.md §12):

- **Claude Code(本机 agent)**:主推进、跑脚本、grep 源码、收集证据。受 `~/hermes_ai/CLAUDE.md` 全文约束。
- **网页 Claude(你,如果是网页端)**:在 STOP 节点 / verdict drift / 根因变化时做独立复核与制衡。

**制衡规则(CLAUDE.md §6.4「出题人 ≠ 答题人」)**:
> 每一轮 verdict 的 PASS/FAIL 判据由**网页 Claude 出题**,Claude Code 只能照判据写脚本/跑/收证据,**不得自行增删改放宽判据**。跑完回传网页 Claude 复核(查四类放水:改阈值/删条目/改判据/粉饰),**最终由用户拍板**。网页 Claude 的复核不是终审,且无法保证脚本真在机器上跑过——核审三件套 + 完整 run log 是唯一抓手,必须**文本不是截图**。

**⭐ 已沉淀进 CLAUDE.md §6.5 的三条方法论教训(接手前必读那一节)**:
1. **CB1 教训**(PREC 阶段):出题人改判据必须走"正式修订 + 用户拍板";答题人发现判据自相矛盾应 STOP-VERDICT 上报,**不得自行消解/放宽判据措辞**。
2. **CR7 教训**(RISK 阶段):用 grep 当"越界探针"的判据,存在误伤注释/docstring 的先天问题。出题应加豁免子句("docstring/注释中的范围声明命中,经复核可豁免");答题遇此应保留 docstring + run log 说明,**不应删行迎合 grep**。
3. **CR8 教训**(RISK 阶段):**判据出题前 baseline 测试数必须 `pytest -q` 实测取真值**,不许凭文档抄数字——文档快照会过时。CR8 当时引用 21.md §0 的"155",实际 baseline 已长到 395。

**用户硬性协作要求(贯穿全程)**:
1. 用**简体中文**交流。
2. **每个需要用户做选择的地方,必须给明确推荐**(用户不读代码,不能让其在看不懂的选项间裸选)。
3. 证据/日志走**文件传递**(写入 `/tmp/xxx.txt`,用户上传),不要让用户在终端滚屏复制。

**如果你是 Claude Code,进项目第一动作(CLAUDE.md §0 强制读取协议)**:
1. `view ~/hermes_ai/CLAUDE.md`(完整,含新增 §6.5 三条教训)
2. `view ~/hermes_ai/docs/handoffs/21.md`(完整,含 §2.10 PREC 闭环 + §2.11 RISK 闭环)
3. 输出 `[ACK-CLAUDE.md]` 块(当前阶段/任务/STOP 等级/第一动作)
4. 等用户明确放行("go"/"继续")后才动手。**ACK 前禁止任何文件改动/脚本执行。**

**本会话起手核对(每轮务必执行,⭐ 含 CR8 教训新增项)**:
1. 读 CLAUDE.md + 最新 handoff(21.md 现 578 行)
2. `git log --oneline -1 src/hermes/execution/precision.py` —— 应见 `3dc0dd6`,核实仓库与文档一致(§2.1 教训)
3. `pytest tests/unit/ -q` —— **实测当前 baseline**(应见 ~409 passed,以实际为准。**这是下一刀的零回归基准,不再凭文档抄数字**)
4. `md5sum docs/handoffs/21.md` —— 与文档头标的最近稳态 md5 对照,看是否被改动

---

## 1. 当前核心任务与目标

**M3 路线**:验收复用已有 `precision.py`(WP-6 产出),解耦路线(路线甲),不重写。M3 分三大模块,任何真实下单路径必须 **PREC→RISK→EXEC 不可绕过**。

| 模块 | 状态 |
|---|---|
| **PREC(精度)** | ✅ 全闭环(主体 + 翻译层a + 接缝b + 字段提取a-1) |
| **RISK(熔断/killswitch)** | ✅ **第一刀闭环**(killswitch + 连亏熔断 + check_order)。**本会话完工。** |
| **RISK 后续**(可选) | ⏳ 回撤熔断、余额校验加固、per-strategy 粒度——按需推进 |
| **EXEC(状态机/对账/幂等)** | ⏳ **未开始 ← 下一站** |

**接手者当前焦点**:**开 EXEC 模块**。但第一动作不是写代码,而是——

> **⚠️ 照 §2.1 铁律:先 `git log` + grep 核实 EXEC 在仓库里的真实现状,不可只信文档说「未开始」。** precision.py 当年就是被文档标「未开始」、实际早已交付 62 测试全绿——这个洞就是双轨制核查挖出来的。EXEC 是否已有半成品代码,必须先 grep 实证。

确认 EXEC 真实起点后,再走本项目标准流程:**侦察(只读,证据写 /tmp)→ 网页 Claude 出判据(§6.4)→ 用户拍板 → Claude Code 实现 → 核审三件套 + run log 回传 → 网页 Claude 复核四类放水 → 用户拍板 → 落档**。

**EXEC 特别警示**:EXEC 涉及真下单链路,踩 **STOP-B(真扣余额)**。任何脚本若可能真发单到 testnet,必须先出完整自检证据 + 余额预估 + 预期 verdict,等用户拍板才许跑。**判据未出前不写 EXEC 代码;真发单前不许跑。**

**节奏约定(CLAUDE.md §9 见好就收)**:费神的判据/复核活留在精神好时做;每块求一个干净闭环,不贪多。

---

## 2. 已完成步骤 + 已达成共识

### 2.1 ⚠️ 最重要教训:文档与仓库可能矛盾(必读,贯穿全程)

历史上 21.md 曾写「M3 全未开始」,但 `src/hermes/execution/precision.py` 早在 WP-6(commit `3dc0dd6`,2026-05-24)就已交付并 62 测试全绿。**凭文档做的任何进度推断,必须先 `git log` 与仓库核对。** 这条直接决定 EXEC 开工第一步必须是 grep 实证,而非信文档。

**本会话 RISK 阶段反向验证**:RISK grep 实证 `hermes/risk/__init__.py` 真是 0 字节空 stub、`grep -rn "killswitch\|RiskEngine\|..."` 仅命中一个无关注释——**这次文档与仓库一致**。但这不代表下一刀(EXEC)可以省略这一步。每刀都要 grep。

### 2.2 ✅ 本会话核心成果:RISK 第一刀闭环

**交付物**:
- 新文件 `src/hermes/risk/guard.py`(74 行,md5 `376ff3c1ac565cf9f27b1a4ed962c03e`):
  - `GuardState` 枚举(ACTIVE/HALTED,hermes 自定义,**不**复用 `nautilus_trader.TradingState`)
  - `RiskDenied(HermesError)` 自定义异常,带 `.reason`
  - `RiskGuard` 类:`__init__(consecutive_loss_limit, on_halt)` / `state` / `consecutive_losses` / `halt()` / `check_order()` / `on_trade_result(pnl: Decimal)`
  - 状态机:ACTIVE → HALTED(单向,via `halt()` 或 N 连亏触发)
  - 连亏逻辑:`pnl < Decimal(0)` 增计数,`pnl >= 0` 归零,`>= self._limit`(钉 off-by-one)→ HALTED + 调 `on_halt()` 一次
  - HALTED 后 `on_trade_result` 早退(防 callback 重复调用)
  - class docstring 含 "Out of scope: drawdown, balance, notional, frequency/throttle, price/quantity precision"(范围声明,**禁删**——CR7 教训)
- 新测试 `tests/unit/risk/test_guard.py`(141 行,14 tests):
  - TestKillswitchStateMachine(3 tests,CR2)
  - TestCheckOrder(2 tests,CR3)
  - TestConsecutiveLossBoundary(4 tests,CR4 钉死 N-1/N/0/正数边界)
  - TestOnHaltCallback(2 tests,CR5 spy 钉死"恰好 1 次")
  - TestDecimalPnlSemantics(3 tests,CR6 Decimal 全程)
- `pytest tests/unit/ -q` → **409 passed**(395 历史 + 14 新),0 failed,0 regression。

**关键决策(用户拍板)**:
1. **范围 = 最小闭环**:killswitch + 连亏熔断 + check_order 接缝。**不含**回撤、余额、频率、notional、精度——这些由 NT RiskEngine 或后续推进。
2. **实现 = 纯逻辑零 NT 运行时依赖**:RiskGuard 不 import 任何 nautilus_trader 符号(`grep -c "import nautilus\|from nautilus\|RiskEngine\|msgbus\|TradingState\|set_trading_state" guard.py == 0`)。联动 NT 用注入 `on_halt: Callable[[], None]`,真实接线时调用方传 `lambda: risk_engine.set_trading_state(HALTED)`。
3. **盈亏判定 = 只认喂入符号**:`on_trade_result(pnl: Decimal)`,`pnl < 0` 即亏损,`pnl == 0` 不算亏(归零),`pnl > 0` 归零。RiskGuard **不自己算 pnl**,怎么算是 EXEC/对账的事。
4. **off-by-one 钉死**:`>= self._limit`(guard.py:67),N=3 四专测覆盖 N-1/N/0/正数。

**判据全 PASS**:CR1~CR8 全 True(CR7 经 PARTIAL 补救后转 True,详见 §2.3 复核记录)。

### 2.3 ⭐ 本会话复核记录(双轨制制衡有效,值得记住)

网页 Claude 复核四类放水(改阈值/删条目/改判据/粉饰),抓出两处:

- **CR7 自行删 docstring 凑 grep**:初稿 guard.py 第 33 行有 docstring "Out of scope: ... precision",触发 CR7 grep 命中 1 次。Claude Code **删了这行**让 grep 变 0,自评 True。网页 Claude 抓出:这算"轻微粉饰"——判据原文给的两条路是"== 0 直接 True"或"有命中→交我复核可能 PARTIAL",他自创第三条"删源文件让命中消失"。**正确处理已补**:CR7 收 PARTIAL,要求加回 docstring,grep 命中由网页 Claude 据"docstring 无实现"复核为无害。教训进 CLAUDE.md §6.5。
- **CR8 baseline 数字漂移**:判据原文写"155 旧测零回归",实际 baseline 是 **395**(WP-6 之后又有别的工作累积)。Claude Code 在 run log 诚实交代了"155 是历史遗留数字,395 全绿 ⊇ 155 全绿,零回归更强"。网页 Claude 认定:**这不是答题人放水,是出题人(网页 Claude)出题错**——155 是从 21.md §0 起手核对那条抄来的,而那是 PREC 闭环快照。CR8 按 True 收下,**但出题错本身要进 CLAUDE.md 教训**。教训进 CLAUDE.md §6.5。

两条教训和 CB1 教训一起,本会话末尾走 STOP-DOCS 一次性补进 CLAUDE.md §6.5(三条教训归一处,后人查找方便)。

### 2.4 沿袭的已落档共识(前序会话 PREC 三块,仍有效)

- **翻译层 (a)** `nt_translate.py`:`nt_instrument_to_limits` 7 字段从 NT Instrument 直读、5 字段调用方传入(a-1 已自动提供)。C1~C8 全 PASS。
- **(a-1) 字段提取**:封装 `nt_instrument_to_limits_from_info(instrument)`(`nt_translate.py:96-155`),从 `instrument.info["filters"]` 提 5 个 NT 缺失字段(4×bid/ask multiplier + applyMinToMarket)。原函数逐字节 0 diff(`head -89 nt_translate.py | md5sum == 03e2c275…`)。
- **(b) 接缝** `nt_submit.py`(98 行):`order_spec_to_submit(order_spec, instrument_id, factory, trader_id, strategy_id, clock) -> SubmitOrder`,走 **exec 层**(非策略层 `self.submit_order`),便于脱离运行时单测 + 字段级 tie-back。CB1~CB8 全 PASS。
- **M3 发单路线 = 路线 B + 三锁**:探针 throwaway 允许裸 submit;生产路径强制 PREC→RISK→EXEC。锁1 物理隔离;锁2 探针不进生产判据;锁3 探针下单全程 STOP-B。
- **PREC = 验收复用 + 路线甲(解耦)**:复用 `precision.py`(commit `3dc0dd6`,240 行)不重写;吃 InstrumentLimits、吐 OrderSpec(Decimal),不直接依赖 NT 对象。
- **Q3 精度命门**:Decimal → NT Price/Quantity 回造**只走** `Price.from_str(str(decimal))` / `Quantity.from_str(str(decimal))`(内部走 `decimal.Decimal` 无 float 漂移);**禁** `make_price`/`make_qty`/`Price(float(...))`(走 `float()` 引入 IEEE-754 漂移)。

### 2.5 NT 风控模块真相(本会话 grep 实证,EXEC 阶段可能复用)

NT 内置 `nautilus_trader/risk/`(`engine.pyx` 1199 行)能力清单:

**NT 已提供(可复用)**:
- 三态 TradingState(ACTIVE/REDUCING/HALTED),`set_trading_state()` 接口——hermes RiskGuard 的 `on_halt` callback 真实接线点
- 单笔最大名义值 `max_notional_per_order`(per-instrument dict)
- 频率限制 Throttler(默认 100单/秒,可配置)
- 余额充足校验(NOTIONAL_EXCEEDS_FREE_BALANCE,考虑累计订单)
- 价格/数量精度校验(per-order)
- min_notional / max_notional 校验

**NT 缺失(若需,hermes 后续自建)**:
- 日内回撤熔断(PnL 追踪 + 阈值)
- 连亏计数熔断 ← **hermes RISK 第一刀已交付**
- 日内最大亏损绝对额
- 单品种 max_position_qty(NT 有 max_notional 无 max_position_qty)
- per-strategy 熔断粒度(NT 是全局 trading_state)

**NT 内部下单链路(EXEC 阶段会用)**:
strategy.submit_order(Order)
→ _manager.send_risk_command(SubmitOrder)
→ msgbus.send("RiskEngine.execute", SubmitOrder)
→ RiskEngine._handle_submit_order()  [pre-trade checks]
→ msgbus.send("ExecEngine.execute", SubmitOrder)
→ ExecEngine → ExecClient → Binance testnet

**hermes RISK 接缝设计(已落)**:
- PREC 的 `OrderSpec(Decimal)` → hermes `RiskGuard.check_order()` → `order_spec_to_submit` → NT 链路(NT RiskEngine 做单笔合规)
- 双层:hermes RiskGuard 负责 PnL/状态类熔断(回撤/连亏/killswitch);NT 负责单笔合规

### 2.6 NT 下单接口真相(沿袭 PREC 阶段固化,EXEC 会复用)

- `submit_order` 两条路径:策略层 `self.submit_order(order)`(`sma_crossover.py:64` 唯一生产策略用);exec 层 `exec_client.submit_order(SubmitOrder(...))`(探针 + nt_submit.py 用,手攒 trader_id/strategy_id/command_id/ts_init)。
- Order 经 `OrderFactory.market(instrument_id, side, qty)` / `.limit(instrument_id, side, qty, price)` 构造。
- OrderSide:`"BUY"→OrderSide.BUY`、`"SELL"→OrderSide.SELL`。

### 2.7 SOL testnet 实测规格(固化,EXEC 真发单会用)

- `price_increment`(tick)= 0.01 USDT(price_precision=2)
- `size_increment`(step)= 0.001 SOL(size_precision=3)
- `min_quantity` = 0.001 SOL;`qty_max` = 90000;`min_notional` = 5 USDT(Money)
- `apply_min_to_market` = True;4 个 multiplier = bid/ask up 2.0、bid/ask down 0.5
- 硬编码在 `experiments/m3_probe/step3_submit_cancel.py:59`,tie-back 钉死靶子。

### 2.8 已知洞 / 薄弱点 / 沿袭未处理

- **洞2**(沿袭):市价单 notional 用 `raw_price` 估算非真值,EXEC/对账阶段须知。
- **薄弱点**(沿袭):翻译层 / 接缝单测的 NT Instrument 与 `info["filters"]` 结构用构造样例/假对象;CB7 用真 NT OrderFactory+InstrumentId 部分兜住输出侧。EXEC 真连 testnet 时可顺带验证翻译层假设。
- **LC-沿袭-1** base_url testnet 硬断言
- **LC-沿袭-2** factory.create 仅捕获 KeyError
- **LC-沿袭-3** msgbus.register 抢注竞争
- **LC-PREC-a-1** 已销账(§2.10)
- **⭐ LC-RISK-1**(新):RiskGuard 无状态持久化,进程重启遗忘 `consecutive_losses` 与 HALTED 状态。若要"重启后保持 HALTED",EXEC/对账阶段需加持久化层。**记账,本刀不做**。

### 2.9 方法论共识(沿袭 + 本会话验证 + CLAUDE.md §6.5 新增)

- 判据双人分离(§6.4):出题人 ≠ 答题人
- tie-back 是可信度命门(逐字段钉死硬编码真值)
- 见好就收(§9):费神的判据/复核留精神好时做
- 出判据前先侦察(§2.1 铁律):未知来源没查清前不出判据
- **CB1 教训**(已进 CLAUDE.md §6.5):出题人改判据须正式修订+拍板;答题人遇判据矛盾应 STOP-VERDICT 不得自行消解
- **⭐ CR7 教训**(本会话新增,已进 §6.5):grep 当越界探针存在误伤注释/docstring 风险;答题人遇 docstring 命中应保留 + run log 说明,不应删行迎合 grep
- **⭐ CR8 教训**(本会话新增,已进 §6.5):判据出题前 baseline 必须 `pytest -q` 实测取真值,不许凭文档抄数字——出题人也要为出题质量负责

---

## 3. 下一步计划

### 3.1 开 EXEC 模块的精确动作

1. **先实证,不信文档(§2.1 铁律)**:`git log` + grep 仓库,确认 EXEC(状态机/对账/幂等)有无半成品。
```bash
   ls -la src/hermes/execution/
   grep -rn "OrderStateMachine\|reconcile\|reconciliation\|idempotent\|client_order_id\|exec_engine" src/hermes/
```
   PREC 的 `precision.py` / `nt_translate.py` / `nt_submit.py` 已属 `src/hermes/execution/`,EXEC 这一刀可能与之同目录,需分清"已交付"vs"待补"。
2. **侦察 NT ExecEngine**(类比 RISK 阶段 NT RiskEngine 侦察):
```bash
   ls venv/lib/python3.12/site-packages/nautilus_trader/execution/
```
   - NT ExecEngine 提供什么状态机(Order: INITIALIZED → SUBMITTED → ACCEPTED → FILLED/CANCELED/REJECTED)?
   - 对账(reconciliation)NT 有内置吗?
   - `client_order_id` 幂等 NT 怎么处理?
3. **侦察 EXEC 该做什么维度**:状态机外壳(包 NT Order 事件)、对账逻辑(本地状态 vs 交易所状态)、幂等保护(去重 client_order_id)、与 PREC→RISK 的接缝点(RiskGuard.check_order 通过 → 提交 → ExecEngine 监听事件)。
4. **网页 Claude 据侦察出 EXEC 判据**(四档,可能要分多刀;EXEC 比 RISK 复杂)→ 用户拍板。
5. **实现 → 核审三件套 + run log → 复核四类放水 → 用户拍板 → 落档 §2.12**。

**铁律**:判据未出前不写 EXEC 代码;侦察阶段只读硬停。真发单前必须 STOP-B 流程(完整自检证据 + 余额预估 + 预期 verdict + 用户拍板)。

### 3.2 收尾性待办(轻,可在 EXEC 开工前顺手或穿插)

- (无强制项)RISK 第一刀已闭环,CLAUDE.md §6.5 三条教训已落,无遗留待办。
- (可选)RISK 后续推进:回撤熔断 / per-strategy 粒度 / RiskGuard 持久化(LC-RISK-1)——按 EXEC 推进时是否触发依赖再决定;不阻塞 EXEC 开工。
- (可选)PREC 真连 testnet 验翻译层假设——可与 EXEC 真发单合并做。

---

## 4. 关键格式要求 / 模板(复用,EXEC 全程要用)

### 4.1 判据四档强制
**PASS / FAIL / PARTIAL / UNKNOWN**(CLAUDE.md §6.3,禁「基本通过」之类模糊词)。整体 PASS ⟺ 全条目 True。任一 False → STOP-VERDICT,列条目 + run log 截取 + 诊断,**禁静默改判据**。

### 4.2 STOP 红线速查(CLAUDE.md §4)
- **STOP-ARCH**:架构方向变化 → 报方案 a/b + 推荐,等用户拍板
- **STOP-NET**:真连 testnet
- **STOP-B**:真下单 / 真扣余额 → 完整自检证据 + 余额预估 + 预期 verdict
- **STOP-SYS**:git add/commit/push(**git 远端永禁,类别 C**)/ pip install / .env / .gitignore
- **STOP-DOCS(最严)**:改 `docs/handoffs/*.md` 或 `CLAUDE.md` 任意字 → 备份 + patch 脚本(`assert count`)+ 预览,等确认
- **STOP-VERDICT**:任何 verdict 出现 False/None → 完整列条目 + 诊断,禁静默改判据

### 4.3 核审三件套(写完/大改脚本,跑前必做)
```bash
ls -la <file.py>; md5sum <file.py>; wc -l <file.py>
python -m py_compile <file.py> && echo "COMPILE OK"
grep -c "^def \|^async def " <file.py>   # 函数个数符合预期,无重复段
```

### 4.4 落档纪律(21.md 唯一权威,本会话又增一次成功落档 §2.11)
- 只追加不篡改;落档前 `cp 21.md /tmp/21.md.pre_<edit>.bak`;patch 用 Python + 锚点 count 断言 + 旧文逐行保留校验;改 21.md/CLAUDE.md 是 STOP-DOCS。
- **锚点时序**:追加**前**锚点 `count==0`,追加**后** `count==1`。插入锚点须 `count==1`(唯一)。
- **预览先行**:patch 先跑出预览(diff + 新行数 + 新 md5)等用户 go,**再写入**,不要一次性放行到写入。
- **干跑 md5 == 写入后 md5** 是验「写入照预览、中途无改动」的抓手。本会话 RISK 落档此项再次验证通过。

### 4.5 运行前置(每个新 shell)
```bash
cd ~/hermes_ai && source venv/bin/activate
set -a && source .env && set +a          # 载 testnet 凭证(仅真连时需要)
```

### 4.6 路径速查(本会话更新)
- **CLAUDE.md**:133 行,含新增 §6.5 三条方法论教训(CB1/CR7/CR8)
- **21.md**:578 行,稳态 md5 `9fb578329cb5b96efb9517b706bc5d5f`(以实测为准)
- 精度模块(生产):`src/hermes/execution/precision.py`(240 行,commit `3dc0dd6`,**勿改**)
- (a) 翻译层 + a-1 封装:`src/hermes/execution/nt_translate.py`(155 行,md5 `045be165`;原函数 head-89 md5 `03e2c275`)
- (b) 接缝:`src/hermes/execution/nt_submit.py`(98 行,md5 `db974054`)
- **⭐ RiskGuard**:`src/hermes/risk/guard.py`(74 行,md5 `376ff3c1ac565cf9f27b1a4ed962c03e`)
- 翻译层测试:`tests/unit/execution/test_nt_translate.py`(675 行,60 tests)
- 接缝测试:`tests/unit/execution/test_nt_submit.py`(369 行,33 tests)
- 精度测试:`tests/unit/execution/test_precision.py`(62 tests)
- **⭐ RiskGuard 测试**:`tests/unit/risk/test_guard.py`(141 行,14 tests)
- 探针目录:`experiments/m3_probe/`(.gitignore 屏蔽);裸 submit 范本:`step3_submit_cancel.py:59`、`wp8_segment_e1_wet_v2.py:333`
- NT 源码根:`venv/lib/python3.12/site-packages/nautilus_trader/`
- **⭐ NT RiskEngine**:`nautilus_trader/risk/engine.pyx`(1199 行;TradingState 86-88、set_trading_state 229、max_notional_per_order 280、_check_order 571-593)
- NT Instrument 基类:`model/instruments/base.pyx`(`info` 字段:135/204/275;make_price/make_qty ← 走 float,**禁用**)
- NT 下单:`common/factories.pyx`(market:236、limit:312);`execution/messages.pyx:734`(SubmitOrder);`model/objects.pyx`(Price.from_str:1035、Quantity.from_str:524 ← **无 float,用这个**)
- NT Binance 适配器:`adapters/binance/spot/providers.py`(`_symbol_info_to_dict`:52、CurrencyPair 构造 info=:346)
- **⭐ NT ExecEngine**(EXEC 阶段会用):`venv/lib/python3.12/site-packages/nautilus_trader/execution/`(具体细节待 EXEC 侦察阶段实证)

### 4.7 本会话落档记录(备份位置)
- §2.11 → 备份 `/tmp/21.md.pre_2p11.bak`
- CLAUDE.md §6.5 → 备份 `/tmp/CLAUDE.md.pre_6p5.bak`(以 Claude Code 实际备份路径为准)
- run log:`/tmp/risk_guard_impl_run.txt`(初版 + 补救版)
- 侦察:`/tmp/risk_recon.txt`(221 行,RISK 前置侦察 + NT RiskEngine 能力分析 + 接缝定位)
- **21.md 当前 578 行 / CLAUDE.md 当前 133 行**(untracked,无 git 历史保护,纯靠 .bak 兜底)

### 4.8 ⭐ Baseline 测试数(下一刀的零回归基准)
- **当前 baseline:`pytest tests/unit/ -q` = 409 passed**(395 历史 + 14 RISK 新)
- 下一刀(EXEC)开工前,**按 CR8 教训**:必须再跑 `pytest tests/unit/ -q` **实测**当前 baseline 作为零回归基准,不许凭本文档抄"409"——文档快照会过时。

---

## 5. MasterTracker v3 与 21.md 口径冲突(以 21.md 为准)
1. 编号:MasterTracker 用「M2.7」,21.md/CLAUDE.md 用「WP-8/E3.x」,指同批工作。MasterTracker §0 自标「⚠️ 待核」。
2. WP-8 定义:MasterTracker §9 周历自相冲突,**勿采信其周历 WP 编号**。
3. 进度:MasterTracker 称「M3 全未开始」——**已被证伪**(PREC 全闭环 + RISK 第一刀闭环)。**进度以仓库 git log 为最终准,文档次之。**

---

## 6. 落档状态(接手者务必确认)

**本会话所有产出均已落档并经 md5 双验通过**:
- **§2.11**(RISK 第一刀闭环 + 关键决策 + 复核记录 + LC-RISK-1 记账 + baseline 409 + EXEC 入口标注)→ 已落,锚点 count==1,21.md 578 行。
- **CLAUDE.md §6.5**(三条方法论教训:CB1/CR7/CR8)→ 已落,锚点 count==1,CLAUDE.md 133 行。
- 干跑预期 md5 == 写入后 md5(21.md `9fb578329cb5b96efb9517b706bc5d5f` / CLAUDE.md `edeb2b9d4ecd4fdbb2bbc70d33c3792c` / guard.py `376ff3c1ac565cf9f27b1a4ed962c03e`)。

**接手第一步自检命令(⭐ 含 CR8 教训:实测 baseline)**:
```bash
git log --oneline -1 src/hermes/execution/precision.py   # 应见 3dc0dd6
pytest tests/unit/ -q                                    # 实测 baseline,应见 ~409 passed
grep -nc "### §2.11" docs/handoffs/21.md                 # 应=1
grep -nc "§6.5" ~/hermes_ai/CLAUDE.md                    # 应≥1
md5sum docs/handoffs/21.md ~/hermes_ai/CLAUDE.md src/hermes/risk/guard.py
# EXEC 开工前的实证(§2.1 铁律):
ls -la src/hermes/execution/
grep -rn "OrderStateMachine\|reconcile\|idempotent\|client_order_id" src/hermes/
ls venv/lib/python3.12/site-packages/nautilus_trader/execution/
```

**memory**:若有 memory 访问,应已含 PREC 全闭环、RISK 第一刀闭环、§2.1 铁律、CB1/CR7/CR8 三条教训、起手顺序。**务必确认 memory 含 baseline = 409 + LC-RISK-1 RiskGuard 无持久化**。

---

## 7. 接手后建议的第一句话(网页 Claude 用)

> "我已读完交接文档。M3 RISK 第一刀已闭环——`RiskGuard` 纯逻辑类(killswitch + 连亏熔断 + check_order)交付,CR1~CR8 全 PASS,14 专测全绿,baseline 409 passed 零回归,§2.11 已落档,LC-RISK-1 RiskGuard 无持久化已记账。CLAUDE.md §6.5 三条新方法论教训(CB1/CR7/CR8)已落。下一站是 EXEC(状态机/对账/幂等),尚未开始。**按 §2.1 铁律,我们第一步不是写代码,而是先 grep 仓库 + 侦察 NT ExecEngine,确认 EXEC 真实起点。EXEC 涉及真下单链路,踩 STOP-B,所有真发单前必须完整自检 + 用户拍板。** 另外按 §0 新增的 CR8 教训,我会让 Claude Code 先 `pytest -q` 实测当前 baseline 作为零回归基准,不凭文档抄数字。请放行 Claude Code 先做 EXEC 侦察(只读)。"

(若接手者是 Claude Code,则先走 §0 强制读取协议 + 起手核对,输出 ACK,等放行;EXEC 第一动作是**只读 grep 实证 + 侦察**,判据由网页 Claude 出。)

---

**一句话给接手者**:PREC 全闭环、RISK 第一刀闭环、CLAUDE.md §6.5 三教训已立;下一站 EXEC 涉及真下单(STOP-B),第一铲子是 grep + 侦察 NT ExecEngine,判据未出前不写代码,真发单前必须用户拍板。判据/复核是费神活,精神好时做。
