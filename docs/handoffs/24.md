# 交接 24 — STOP-B Phase 1 cancel 路径首秀 CLEAN / 教训 STOP-DOCS-1 / 下一站候选

> 接手者：先读 §0 → §1 红线（承 21.md）→ §6 起手清单。本文档自洽。
> **当前实际状态：STOP-B Phase 1 cancel 路径首秀闭环（CB1~CB12 全 PASS，§2.17 已落 21.md 888 行）。成交路径（Phase 2 填仓）未做。下一站待主理人决断。**

---

## §0 背景定位

- 承 21.md 全部定位（WP-8 全段闭环 / M3 PREC+RISK+EXEC 模块 / STOP-B 首秀框架）。
- 本会话唯一新产出：STOP-B Phase 1 cancel 路径首秀（→ 21.md §2.17）。
- ⚠️ **防认知污染**：Phase 1 = cancel 路径（远端限价单挂单 → ACCEPTED → cancel → CANCELED），**不是成交，无 SOL 净变化**。Phase 2 填仓（market BUY 真成交，真扣 SOL）**未做**，挂账待主理人发起。

---

## §1 纪律红线（承 21.md §1 全部条款）

- §1.1–§1.8 承 21.md 不变，含落盘后三条实证核审、STOP 分类、凭证泄漏防护、git 纪律。
- §1.8 补充：`experiments/m3_probe/` 与 `docs/handoffs/` untracked；本地领先 origin 1 commit（3dc0dd6）—— **不要碰**。

---

## §2 本会话核心产出

### §2.1 STOP-B Phase 1 cancel 路径首秀（2026-05-29）

详见 21.md §2.17。以下为摘要。

**脚本**：`experiments/m3_probe/stopb_phase1_cancel.py`（511 行，md5: e65737ae785f5ddad7206a7a48a815a1，`.gitignored`）

**真实 Binance testnet 订单数据**：
- venue_oid: **1201712**（Binance testnet 真返回）
- 发单参数（运行时重算）: price=40.97 USDT / qty=0.146 SOL / notional=5.9816 USDT
- 参照价: MIN_1 kline close = 81.86 USDT；PPBS 合法区间 [40.93, 163.72]（三道安全闸全通）
- status 流: SUBMITTED → 6(ACCEPTED, 1.00s) → cancel → 8(CANCELED, 1.00s)
- timing: after-accept cancel（§7.1 B1 教训 诚实标注）
- 余额: SOL 5.3→5.3 / USDT 9974.7274→9974.7274（|Δ|=0，cancel 路径无变化）

**CB1~CB12 全 PASS**（网页 Claude §6.4 出题，四类放水零命中）。

**21.md 落档**：888 行 / md5 c067540bfa38ae44fde6ddc9b59a698d / §2.17 anchor=1。

---

## §3 教训（本会话新增）

### 教训 STOP-DOCS-1（STOP-DOCS 阶段，2026-05-29）

§2.17 落档跳过"预览先行"闸口——直接写入，无预览 md5，导致写入后 md5 无对照基准。事后靠"逐字回贴核验"兜底，内容查实 CLEAN，但预览→写入的字节级抓手永久缺失。

**下次 STOP-DOCS 必须**：patch 先出干跑预览（diff + 新行数 + **预览 md5**）→ 等放行 → 写入 → 写入后 md5 必须 == 预览 md5。

**待补**：此教训应补进 CLAUDE.md §6.5（像 CB1/CR7/CR8/EXEC-DRIFT 那样），下次开会话单独走一刀 STOP-DOCS 落它。

### 附带教训（"go" 歧义，2026-05-29）

网页 Claude 问"要我先草拟正文吗"、用户答"go"时，"go" 的指向有歧义（放行落档动作 vs 放行网页 Claude 起草）。以后网页 Claude 出 SUMMARY/正文应先草拟再问 go，不给歧义留空间。

---

## §4 当前状态 / 基线

### §4.1 测试基线

`pytest tests/unit/ -q` = **468 passed**（0 failed；flaky `test_reconnect_exponential_backoff` 本次通过，FLAKY 性质未变；CB10 修订判据满足）

### §4.2 五基线 md5（发单前后双验零漂）

| 文件 | md5 |
|---|---|
| `src/hermes/risk/guard.py` | `6d13bfb2608945f8ba9311b3d36bcfb5` |
| `src/hermes/execution/precision.py` | `bb0308ce6cf0b50f5214e5d9819f2cc8` |
| `src/hermes/execution/nt_translate.py` | `045be1651ba9b0f0561d66c8f3a10cd5` |
| `src/hermes/execution/nt_submit.py` | `db974054f08f4a15ee3357cd74a565cb` |
| `src/hermes/execution/exec_bridge.py` | `5b4453fc227a7da079c89f5446e35ffb` |

### §4.3 LC backlog（未决，不挡主线）

- **LC-WS-1**：`test_reconnect_exponential_backoff` 未完全 mock 隔离，触及真实 testnet WS 连接（FLAKY）。
- **LC-EXEC-1**：`OrderIdFactory.seen` 无跨进程持久化。
- **LC-EXEC-2**：`OrderRejected.due_post_only` 未路由策略层。
- **LC-EXEC-3**：跨币种 commission 丢弃 PnL 喂入。
- **LC-RISK-1**：RiskGuard 无状态持久化（进程重启遗忘 streak/HALTED）。
- **远端挂价能力 backlog**：`ppbs_lower × 1.001 + floor_to_tick` 的 far-limit price 算法当前手写在探针脚本，未收进 `src/hermes`。

---

## §5 下一站候选（待主理人决断）

| 候选 | 说明 |
|---|---|
| **STOP-B Phase 2 填仓** | market BUY 真成交，真扣 SOL，STOP-B 红线更重（类别 B）。须重走 CB0 + before 余额 + 新价发现三闸，网页 Claude 出新判据。 |
| **RISK 第三刀** | 日内余额/仓位限制或其他 RiskGuard 扩展。判据由网页 Claude 出题（§6.4），判据未出前不写代码。 |
| **EXEC 第三刀** | PositionEvent extractor 端到端或其他 ExecBridge 扩展。同上。 |
| **清 LC backlog** | 轻量刀，任选一条 LC 销账（如 LC-WS-1 mock 隔离修复）。 |
| **CLAUDE.md §6.5 补教训 STOP-DOCS-1** | 单独 STOP-DOCS 一刀，走预览先行协议。 |

---

## §6 新会话起手清单

1. 读 CLAUDE.md + 本文档（22.md）
2. `git log --oneline -1 src/hermes/execution/precision.py` → 应见 `3dc0dd6`
3. `md5sum docs/handoffs/22.md` → 核实与本文档 md5 一致
4. `pytest tests/unit/ -q` → 取当前真 baseline（勿抄文档数字，承 CR8 教训）
5. 五基线 md5 核实零漂（§4.2 表格）
6. 等主理人明确"做什么 + 不许做什么"，不自驱推进任何任务
