# 49.md — 会话收口：M4-刀1（Telegram 只读运维告警 / monitoring 绿地建包 + Decimal 守原则）

## §0 背景定位
本轮 = M4 起手「刀1：Telegram 只读告警」。承 48.md（HEAD d0b7bfa，baseline 746，V25 方向A 闭 + F 全闭）。
本轮 push 后 HEAD = c1b225c（链：d0b7bfa -> aa744d2 -> c1b225c）。
三方协作模型不变：Iris 主理人（唯一 git 写权）/ Web Claude 复核出题 + 文档起草 / Claude Code 执行（只读取证）。
进度以仓库 git log 为最终准，文档次之（沿袭 22/23.md 铁律）。

## §1 三轮 recon 定 M4 路线（只读）
- recon-1/2/3 实证：monitoring/ 空桩绿地；python-telegram-bot 在 pyproject [monitor] optional extra（未装）；
  CLAUDE.md SS4.6 systemd=STOP-SYS、SS6.8.2 Telegram 进程不 import 发单路径（验收硬判据）。
- recon-0：binance_rest 唯一 POST+signed = new_order（S_forbidden={new_order}）；
  只读面实集 = {get_klines, server_time_ms, ping}（account/open_orders/my_trades/ticker 端点不存在）。
- 关键 reframe：账户类告警（余额/成交/撤单）当前做不了 -- 缺 signed-read 端点 ->
  刀1 缩为"运维 monitor"（连通性/时钟漂移/价格异动），账户告警转刀2。

## §2 刀1 实现闭环（commit aa744d2 + __init__ 收尾 c1b225c ✅）
### 设计（路 A）
独立进程 src/hermes/monitoring/：自己 poll 只读 REST，零碰 live 入口 / binance_rest 源码 / signed 端点。
- config.py: TelegramConfig 镜像 BinanceCredentials（SecretStr token 无默认 + 运维阈值字段 symbol/poll_interval_s/clock_drift_warn_ms/price_move_warn_pct）
- rest_reader.py: 薄只读封装，只调 ALLOW={get_klines, server_time_ms, ping}
- alerter.py: poll 循环 + 三路去抖/re-arm（ping 连续3失败 / drift 超阈 / price delta% 超阈）+ 只读命令 /status /price /ping
- __main__.py: python -m hermes.monitoring 入口，SIGTERM 优雅停
- pyproject: +hermes-monitor scripts 入口

### SS6.8.2 判据据实调整（诚实标）
recon-3 证实查询与 new_order 同模块（同一 BinanceRestClient）-> import 隔离物理不可能 ->
判据从"import-iso"改"call-iso"：AST 断 monitoring/ 全包对 {new_order, _request} 调用计数==0。
逐字核两 AST 测断言集为实集（非空壳），覆盖 Attribute + Name 两种调用形态。

### Decimal 守原则（本轮血泪小回合）
首版 alerter 价格路用 float(klines[-1].close) -- 违 Decimal-not-float。
fixup-Decimal：价格量全 Decimal（AlertSnapshot.latest_price / price_move_warn_pct / change_pct x Decimal(100)）。
fixup-A：删 L146 时间路多余 float()（asyncio wait_for timeout，int 满足 duck-typing）。
终态 grep "float(" 全 monitoring 包零命中。

### 判据 CBP1-9 总判（Web Claude 出题 + raw 逐条核）
CBP1 五基线零漂 / CBP2 774 passed（746+28）/ CBP3 call-iso AST 实集 /
CBP4 TelegramConfig 镜像 / CBP5 rest_reader 白名单 AST / CBP6 三路去抖+re-arm /
CBP7 SIGTERM 优雅停 / CBP8 四类放水零命中 / CBP9 ruff+mypy strict CLEAN -- 全 PASS。

### __init__ 收尾（c1b225c）
aa744d2 把 tests/unit/monitoring/ 四测入库但漏了 __init__.py（留 untracked）。
micro-check 坐实：所有 tests/unit/*/ 子目录均有 __init__.py；pytest 配置 pythonpath=["src"]
无 importlib 模式，默认 prepend 模式下同名测试模块跨目录会 collection 冲突 -> __init__.py 是必需。
补 commit c1b225c（空 marker，parity fix）。

### 复核边界（诚实标）
Web Claude 基于 CC re-emit raw（md5/wc 锚 + 现跑 pytest/ruff/mypy + 逐行源码）逻辑层 + 源码层复核，
非独立跑机器。两 AST 隔离测断言集逐字核为实集（_S_FORBIDDEN={new_order,_request} / _ALLOW={get_klines,server_time_ms,ping}）。

## §3 方法论沉淀（本轮新）
- recon 翻 PLAN：上一版凭直觉"systemd 先"，recon-2/3 实证 Telegram 隔离更干净、systemd 触 STOP-SYS 更重 -> 改 Telegram 先行。复核方 authored 的顺序也会错，recon raw 是裁判。
- "绿地"要 recon 才知深浅：account 端点不存在这条，把刀1 从"账户告警"缩成"运维告警"，避免在 monitoring 绿地刀里偷塞 exchanges signed 端点改动。
- SS6.8.2 据结构现实调判据：同模块 -> 卡调用不卡 import，用 AST 零调用 + 可重跑断言兜实质，诚实标注"据实变更非放水"。
- raw 胜 verdict 全程守：CC 自报"AST violations=[]"不采信，坚持调出断言集源码逐行核"禁集是实集非空壳"，才是 SS6.8.2 真过。
- CLEAN 锚漏项靠 commit stat 兜回：commit 报 9 files 与 add 清单逐一对得上，__init__.py untracked 被 git status 暴露，及时补 -- 锚清单 + porcelain 双核胜单看自报。

## §4 ce9 五基线 + HEAD
五基线零漂（本轮只新增 monitoring/，非基线、C 层）：
guard a0b23f0f / precision bb0308ce / nt_translate a9cd0219 / nt_submit db974054 / exec_bridge 2a92d362。
baseline：746 -> 774（+28 monitoring 单测）。HEAD：d0b7bfa -> aa744d2 -> c1b225c。

本轮交付物（均新增非基线）：
src/hermes/monitoring/{config,rest_reader,alerter,__main__}.py（commit aa744d2）
+ tests/unit/monitoring/{test_import_iso,test_rest_reader,test_config,test_alerter}.py（aa744d2）
+ tests/unit/monitoring/__init__.py（c1b225c）
+ pyproject.toml +1 scripts 行（aa744d2）。

## §5 backlog 现状更新
| 项 | 状态 |
|---|---|
| M4-刀1 Telegram 只读运维告警 | ✅ 本轮 resolved（aa744d2 + c1b225c）|
| STOP-LIVE-MON：刀1 活体验证 | 📋 SS4.6，主理人手敲 pip install -e ".[monitor]" + .env token -> 真 poll/push，go-live 前必做 |
| M4-刀2：账户类告警（余额/成交/撤单）| 📋 需先给 binance_rest 补 signed-read 端点（get_account/get_open_orders/get_my_trades），独立一刀碰 exchanges 核心面 |
| M4-刀3：systemd 守护 | 📋 STOP-SYS SS4.6，须改 node.run 补优雅停 + 持久化配置，独立轮 |
| 快照重冻结（price_min/max 0/0->live）| 📋 fixture-hygiene backlog |
| Python .python-version 3.11.15 ↔ venv 3.12.3 | 📋 CLAUDE.md 对账 backlog |
| v25 _sp=2 策略侧精度耦合 | 📋 留 go-live 前与策略接线一并裁 |
| testnet 残单清理（~6 笔 ETHUSDT@1300）| 📋 开工前手动撤 |

## §6 下一站候选（待主理人决断）
- STOP-LIVE-MON（刀1 活体验证，装依赖 + 填 token，真 poll/push）
- M4-刀2（账户告警，先补 binance_rest signed-read 端点）
- M4-刀3（systemd 守护，STOP-SYS SS4.6）
- DEPLOY-模拟盘 4 周（不可压缩日历时间，最大硬约束）
