# 28.md -- 会话收口：WP-8 GATE-2 mainnet 硬拦建闸完成（2026-06-01）

承接文档：`docs/handoffs/27.md`（STOP-B Phase 3 平仓收口）、`docs/handoffs/21.md`（主进度 SS2.1~SS2.29）。
本文档为本会话收口，供下个会话无缝接手。

---

## 0. 背景定位

- **承接自**：27.md（STOP-B Phase 3 market SELL 平仓闭环，账户 flat）+ 21.md SS2.29
- **本会话产出**：WP-8 段 A —— mainnet 硬拦闸（GATE-2）建闸、装闸、验闸 CLEAN、SS6.8 落档 commit（7a380e2）
- **本文档职责**：收口 WP-8 段 A，记录闸体指纹 + 关键教训 + backlog，供下个会话接手

---

## 1. 红线（承 21.md / CLAUDE.md，不变）

- 绝不 git add/commit/push（改动留工作区，主理人手敲）
- STOP-DOCS：写 docs/handoffs/*.md 前必走预览先行 + 预览 md5 == 写入 md5
- STOP-B：testnet 真发单须明确授权 + STOP-ARCH 约束
- STOP-NET / STOP-ARCH / STOP-VERDICT 等红线照 CLAUDE.md SS4 / SS6
- 重要确认（是否 commit / 是否进入下一子项 / 是否动 src）必须由人显式 yes/go

---

## 2. 本会话核心产出

### WP-8 段 A -- mainnet 硬拦闸（GATE-2）建闸闭环

完整链路：建闸（/tmp 预览）-> md5 装闸对齐 -> 双闸验闸 CLEAN（CC 主体）-> SS6.8 落档 commit（7a380e2）

**闸体内容**：

- hook：`~/.claude/hooks/mainnet-gate.sh`
  - primary grep `HERMES_ENV[[:space:]]*=[[:space:]]*['"]?mainnet`（-Eiq）
  - belt `os.environ['HERMES_ENV']=...mainnet`
  - 命中即 permissionDecision=deny
- settings：`~/.claude/settings.json` 更新为 17 deny（15 git + 2 mainnet：
  `Bash(HERMES_ENV=mainnet:*)` + `Bash(export HERMES_ENV=mainnet:*)`）
  + 双 hook（git-write-gate + mainnet-gate 并存，非替换）

**验闸 CLEAN 铁证（CC 主体）**：

- G2 拦截：`HERMES_ENV=mainnet echo GATE_MAINNET_SELFTEST` -> MAINNET-GATE deny，
  echo 未执行，stdout 无该串，MARKER_G2_OUT_START/END 均未打出
- G1 回归：`git commit --allow-empty -m GATE1_REGRESS_DROP` -> GIT-WRITE-GATE deny，
  HB==HA==8057628，HEAD 未前进
- 控制组 C1：`HERMES_ENV=testnet echo OK` 放行，输出 OK
- 控制组 C2：`grep -rn mainnet src/hermes/` 只读放行（不过宽拦只读 recon）

---

## 3. 当前基线快照

**git 锚**：

| 指标 | 值 |
|------|----|
| origin/main HEAD | 7a380e2 |
| 提交链 | 7a380e2 -> 8057628 -> 240d027 -> 5b89c66 |
| 工作树 | clean |

**CLAUDE.md**：328 行，md5 `9e96c5afbe2126ac59b86ab97665cfac`（含 SS6.6 / SS6.7 / SS6.8）

**闸体指纹（用户级 `~/.claude/`，不随 repo，环境重建须重施）**：

| 文件 | md5 |
|------|-----|
| `~/.claude/hooks/git-write-gate.sh`（GATE-1 hook） | `281c070df935338333d2fb18bfc74976` |
| `~/.claude/hooks/mainnet-gate.sh`（GATE-2 新建） | `02ed4b830d12e93aa5968bf09501477e` |
| `~/.claude/settings.json`（GATE-2 更新后） | `b07a03a1cd0df1501eceb6dff81e88df` |

旧 settings md5（GATE-1 时代 `a8730a2293631e018bdc0b8fb85d91ab`）已作废，以上表为准。

---

## 4. 关键教训（防再犯）

**验闸主体铁律（承 SS6.8.1 作用域铁律，写入带走项 (2)）**：

验闸必须由 CC 发起 Bash 工具调用。Iris 在裸 shell 里跑同样命令，hook 按设计放行 Iris，
会得假绿（看似"没被拦"实为闸按设计不拦主理人）。本会话曾误用 Iris shell 跑 G1/G2，
得到假象后已纠偏，改由 CC 主体跑验闸。后续任何闸的验闸，CC 主体是唯一有效验法。

---

## 5. 未解决 backlog（条件未齐，不动）

**SS6.8.2 = E2.5 签 go 前置（四条构建期契约，全绿才签）**：

1. 启动 live/testnet 撮合节点收口到唯一一条 `hermes <subcommand>`（Click command），无旁路
2. 默认 testnet，mainnet 仅靠显式 `HERMES_ENV=mainnet`，入口不另引开关
3. 子命令名确定即注册进 PreToolUse Bash hook deny（同机制），真单只由 Iris 手敲
4. 行为级验闸（CC 主体）且不真发单——CC 试跑该启动命令须在 exec client 实例化 /
   socket 打开前被 deny

**E2.5->E3 第一步**：接执行层（BinanceExec client + new_order 实现 + testnet exec 起
TradingNode）。接入时按 SS6.8 带走项 (1)：启动子命令名定了即注册进 hook deny + CC 主体
行为级验闸 CLEAN，才轮到 STOP-B。

**CLI 现状（三层证伪铁证，当下无任何活发单路径）**：

- exec node 零命中（grep 全库无 add_exec_client 等入口）
- `new_order` 是桩（exec_bridge.py / nt_submit.py 存在但不触达交易所）
- CLI 模块物理不存在：`ls src/hermes/cli.py` + `ls src/hermes/cli` 双
  `No such file or directory`；entry point `hermes = "hermes.cli:main"`
  （pyproject.toml:89）指向缺失模块，`hermes` 命令启动即崩；框架已选 Click
  （click>=8.1,<9.0，pyproject.toml:58）

**STOP-B 三件套状态**：

| 件 | 状态 |
|----|------|
| 发单闸 CLEAN（段 A mainnet 硬拦） | 已 CLEAN（SS6.8.1）；守发单的那道待子命令注册后建 |
| Iris 显式授权 | 未给 |
| 复核方判据 | 未出 |

三件一件未齐，STOP-B 不触发。

---

## 6. 下个会话接手指引

1. **先复验双闸**：按 SS6.7 复验协议 + SS6.8.1 验闸方案（CC 主体），确认 GATE-1 + GATE-2 仍活
2. **接执行层时**：子命令名注册进 hook deny -> CC 主体行为级验闸 CLEAN ->
   才给 STOP-B 三件套补齐条件
3. **不读本文档就自驱推进**是禁止行为（承 CLAUDE.md 末段会话规矩）
