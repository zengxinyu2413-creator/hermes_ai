# 46.md — 会话收口：F' 单刀（nt_translate 接 live + 消静默 fallback / F 实现闭环、真验留 backlog）

## §0 背景定位

本轮 = backlog「F 单刀」轮，经 recon 重定义为 F'。承 45.md（HEAD 91b8e6f，baseline 738，文档轮收口）。
三方协作模型不变：Iris 主理人（唯一 git 写权）/ Web Claude 复核出题 / Claude Code 执行。
进度以仓库 git log 为最终准，文档次之（沿袭 22/23.md 铁律）。

本轮经六轮 raw recon（R1-R10 + P2）后，44.md 原 F 命题被实证推翻、重定义为 F'：
- 原 F =「nt_translate 接 live + 替 instruments.py L32/33 硬编码 2 + 真 testnet 验」
- recon 实证：instruments.py L32/33 是 **backtest fixture 死代码**（`make_solusdt_binance` 全 src 零 caller），不在 live 链，替它对 live 零影响——原靶子虚。
- 真问题：live 精度走 cli.py 手搓 `_parse_instrument_limits`，含 13 行**静默 fallback 默认值**（缺 filter 时静默用写死值，可能过校验但实际不合规）。
- F' 重定义 = 把 cli 手搓解析器收敛到已测好的 `nt_translate.nt_instrument_to_limits_from_info`，**消灭静默 fallback**，让 hermes 校验与 NT RiskEngine 共用同一 instrument 源。

## §1 F' 实现闭环（commit ee497a1 ✅）

### 裁定与设计（STOP-DECISION D1-D4）
- D1 范围锁「cli 解析器收敛到 nt_translate + 删 fallback」；instruments.py L32/33 与 v25 `_sp=2` 都**出本刀**。
- D2 nt_translate / precision **只读不改**（它们是被调方，已写好测好 CA1-7）→ **零 ce9 基线触碰**。真正改的只有 cli.py（非基线）。
- D3 验证 = 离线固化真 SOLUSDT filters 等价对拍，**不连 testnet、不下单**（真 testnet 验留 backlog P4b）。
- D4-a limits 来源依赖 provider 注入的 `_instrument`；`HERMES_SKIP_INJECT=1` 仅 debug 诊断档，真下单必须有真 instrument。

### skip-inject 变量生命周期（CC 侦察揪出，选项 2）
`_instrument` 只在 `if HERMES_SKIP_INJECT != "1"` 注入块内创建。F' 后 limits→build_order_spec→strategy 链传递性绑死该块。裁定选项 2：
- 真下单路径（注入成功）：limits/order_spec/strategy/add_strategy/node.run() 整链收进 if 成功块内。
- skip 分支：显式 `else` → 打印诊断 → 干净 `return`，**不构造下单对象、不裸奔 add_strategy 触发 NameError**。保留 39.md:67/106 记录的「NT 级 DENIED 诊断信号」语义。

### 交付物（md5/行数实测）
- src/hermes/cli.py（diff: 105 insert / 158 delete）— 删 `_parse_instrument_limits`（50 行）+ 删重复 httpx exchangeInfo 拉取 + 删 import httpx/typing.Any/InstrumentLimits；加 `nt_instrument_to_limits_from_info` import；trade_live 尾部按 if/else 分支重构。
- tests/unit/cli/test_cli.py（连带适配）— 删 TestParseInstrumentLimits（6 测，被测函数物理消失）+ 删 `_mock_exchange_info_response`；新增 `_FakeMoney`/`_fake_live_instrument`；`_invoke_live` mock 改喂 fake instrument、删 httpx.get patch。TestTradeLiveLiveWiring 9 测业务断言**逐条未改**。
- tests/unit/execution/test_cli_limits_wiring.py（新建，261 行）— CF10 冻结快照等价对拍（12 字段）+ 缺 PPBS ValueError。

### 判据 CF1~CF10（Web Claude 出题，复核四类放水零命中）
- CF1 _parse 整函数删净 + 三 import 删 + 新 import 加 — PASS（V3 diff 亲核）
- CF2 序正确：add_instrument(120)→limits(124)→build_order_spec(127)→add_strategy(158)→run(159)，全在 _instrument 后 — PASS
- CF3/CF10c 等价对拍：12/12 字段全等（含 apply_min_to_market，真 NOTIONAL 含 applyMinToMarket:True，双双 True），**如实记全等、不伪造分歧** — PASS
- CF4 cli 体内 fallback 零残留（grep Decimal 兜底零命中） — PASS（核心价值兑现）
- CF5 五基线零漂（改前/改后两测，全 == §6 锚） — PASS
- CF6 zero-regression 746 passed（738+14 新 -6 删） — PASS
- CF7 ruff（门检中修 F401 删函数副作用 + 6 RUF001 自纠新写诊断串全角标点；残 4 RUF003 = 既存债、PLAN 令原样保留）+ mypy strict clean — PASS
- CF8 四类放水零命中（fallback 删=去风险非降阈值；业务断言全留；快照逐字；全等如实记） — PASS
- CF9 test_cli 适配：16 测=22-6，9 wiring 测业务断言逐条在、collection 无 ImportError — PASS
- CF10a/b 冻结快照标源 91b8e6f + 算法体逐字 diff exit 0（防借快照偷改算法） — PASS

### skip-inject 收尾（保留诊断语义）
`HERMES_SKIP_INJECT=1` 旧行为（NT 级 DENIED 诊断信号）保留：skip 分支显式 return、不构造下单对象，不退化为 NameError。dry-run 路径行为旧=新（dry 在更早 return，本就不跑 build_order_spec），无回归（已亲核排除）。

## §2 F 未全闭 — 真 testnet 验留 backlog（go-live 前必做）

F' 闭「nt_translate 接 live + 消 fallback」，但 44.md F 的「真 testnet 验」**未做**：
- 本轮验证用**离线固化 filters 等价对拍**（D3），证「新路在真 filters 下与旧路逐字段等价」，**未发起真 testnet 连接、未真下单验精度被 ACCEPTED**。
- 真 testnet 验（拉真 SOLUSDT exchangeInfo → nt_translate 推导 → 真下单验 venue ACCEPTED）= **P4b，STOP-B 域，须 fresh session**，留 backlog，go-live 前必做。

## §3 方法论沉淀（本轮新）

- **六轮 recon 兑现价值**：R1-R10 + P2 逐层实证，把 44.md「替硬编码 2」的虚靶子证伪、揪出真风险=静默 fallback。动刀前 recon 充分，避免照 44.md 字面动刀踩空。recon 充分不等于实现无盲区（见下）。
- **CC 三次诚实 STOP（执行方处理范围边缘的正确姿态）**：① 侦察揪出 skip-inject 变量生命周期冲突（实盘路径），主动 STOP 交裁不自选方案；② 揪出 test_cli.py 绑死旧架构、不并入则 CF6 验收门自相矛盾，交范围裁决；③ 门检中发现 ruff 11 errors，溯源区分「既存债 vs 本轮引入」、只纠自己新写的、留既存债不碰，自我归类「授权内 vs 需新 PLAN」并主动留撤回口。三次均：不擅扩、不擅缩、诚实披露交裁——§6.1 self-advancing 的正确对立面。
- **对拍叙事让位 raw 事实**：CC 实测真 SOLUSDT NOTIONAL 含显式 applyMinToMarket:True → 两路 apply_min_to_market 全等，主动拒绝为凑 Web Claude 预设的「旧 True/新 False 分歧」叙事而伪造差异。全等是更干净的等价证明，fallback 差异仅边界场景（test_nt_translate 已覆盖）。反放水正向典范。
- **CF10 冻结快照防伪造**：对拍对手方（被删函数）以「标源 + git show 逐字核」的冻结快照顶替，algorithm body diff exit 0 坐实「快照==原函数」，防借快照偷改算法。
- **终端啃花对策复用**：长 raw 经回贴啃花时，diff 的 `+/-` 前缀 + `cat -n` 全文 + `| cat` 防分页 + md5 比对（V1 == fe4b827...）四件套取干净证据，md5 胜过肉眼读花字（承 45.md §4）。

## §4 ce9 五基线 + HEAD

五基线零漂（D2 坐实 nt_translate/precision 只读被调，本轮没碰 src 基线）：
guard a0b23f0f / precision bb0308ce / nt_translate a9cd0219 / nt_submit db974054 / exec_bridge 2a92d362。
baseline：738 → **746**。HEAD：91b8e6f → **ee497a1**。

本轮改动文件（均非基线）：src/hermes/cli.py、tests/unit/cli/test_cli.py、tests/unit/execution/test_cli_limits_wiring.py（新建）。

## §5 backlog 现状更新

| 项 | 状态 |
|---|---|
| ① CLAUDE-REBUILD-FULL | ✅ 主体（45.md）；§2/§3 编号收尾残项 |
| ⑥ MasterTracker v4 | ✅（45.md resolved） |
| F nt_translate 接 live | ✅ 本轮 F' resolved（接线 + 消 fallback）；真 testnet 验未做 → 转 P4b |
| P4b 真 testnet 验 | 📋 新增 backlog，STOP-B 域、fresh session，go-live 前必做 |
| instruments.py L32/33 硬编码 2 | 📋 标注 backtest-only，留 backtest 轮（live 无关） |
| v25 _sp=2 策略侧精度耦合 | 📋 留 go-live 前与策略接线一并裁 |
| CLAUDE.md §2/§3 编号收尾 | 📋 文档轮残项（45.md §3） |
| nt_translate vs nt_order_translate 分工 | 📋 待核（留文档轮） |

## §6 下一站候选（待主理人决断）

- P4b 真 testnet 验（拉真 exchangeInfo → nt_translate 推导 → 真下单验 ACCEPTED，STOP-B、fresh session，go-live 前必做）
- 线 B V25 方向 A（wp_v25_backtest 真 bar 回测，验 PnL/fills + 对拍基准）
- M4 起手（Telegram / systemd）
- CLAUDE.md §2/§3 编号收尾（独立 STOP-DOCS）
- testnet 残单清理（开工前手动撤）