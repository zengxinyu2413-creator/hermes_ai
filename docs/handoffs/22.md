# hermes_ai 进度交接文档 — PREC 全闭环后 → 下一站 RISK

> **本文档面向接手的 AI(网页 Claude 或 Claude Code)。** 读完即可无缝接手,无需追问历史。
> 进度基准 = 项目落档的 `docs/handoffs/21.md`(**当前 544 行,md5 `9601743b5eb4a4fde3b618f12481d2eb`**,untracked 无 git 历史保护,纯靠 .bak 兜底)。
> 用户另有 MasterTracker v3 总规划,**仅供战略参考,不作进度基准**(编号不一致,见 §5)。
> **本会话状态一句话**:M3 精度层 (PREC) 全部闭环——翻译层 (a)、接缝 (b)、字段提取 a-1 三块全 PASS 并已落档 §2.10。**下一站是 RISK(熔断/killswitch),尚未开始。**

---

## 0. 接手者必读:角色与第一动作

本项目采用**双轨制**(CLAUDE.md §12):

- **Claude Code(本机 agent)**:主推进、跑脚本、grep 源码、收集证据。受 `~/hermes_ai/CLAUDE.md` 全文约束。
- **网页 Claude(你,如果是网页端)**:在 STOP 节点 / verdict drift / 根因变化时做独立复核与制衡。

**制衡规则(CLAUDE.md §6.4「出题人 ≠ 答题人」)**:
> 每一轮 verdict 的 PASS/FAIL 判据由**网页 Claude 出题**,Claude Code 只能照判据写脚本/跑/收证据,**不得自行增删改放宽判据**。跑完回传网页 Claude 复核(查四类放水:改阈值/删条目/改判据/粉饰),**最终由用户拍板**。网页 Claude 的复核不是终审,且无法保证脚本真在机器上跑过——核审三件套 + 完整 run log 是唯一抓手,必须**文本不是截图**。

**⭐ 本会话新增的方法论教训(下次开篇应补进 CLAUDE.md)**:
> **出题人改判据必须走「正式修订 + 用户拍板」;答题人(Claude Code)发现判据自相矛盾时应 STOP-VERDICT 上报,不得自行消解/放宽判据措辞。** 本会话 CB1 判据内部矛盾就是靠这条制衡抓出来的(详见 §2.2 复核记录),否则会被一句「全绿」糊过去。

**用户硬性协作要求(贯穿全程)**:
1. 用**简体中文**交流。
2. **每个需要用户做选择的地方,必须给明确推荐**(用户不读代码,不能让其在看不懂的选项间裸选)。
3. 证据/日志走**文件传递**(写入 `/tmp/xxx.txt`,用户上传),不要让用户在终端滚屏复制。

**如果你是 Claude Code,进项目第一动作(CLAUDE.md §0 强制读取协议)**:
1. `view ~/hermes_ai/CLAUDE.md`(完整)
2. `view ~/hermes_ai/docs/handoffs/21.md`(完整,含 §2.8 / §2.9 / §2.10 / §3.4)
3. 输出 `[ACK-CLAUDE.md]` 块(当前阶段/任务/STOP 等级/第一动作)
4. 等用户明确放行("go"/"继续")后才动手。**ACK 前禁止任何文件改动/脚本执行。**

**本会话起手核对(每轮务必执行)**:
1. 读 CLAUDE.md + 最新 handoff(现 544 行)
2. `git log --oneline -1 src/hermes/execution/precision.py` —— 应见 `3dc0dd6`,核实仓库与文档一致(**§2.1 教训:文档曾与仓库矛盾**)
3. `pytest tests/unit/execution/ -q` —— 确认 **155 tests 绿**(62 precision + 38 翻译层a + 22 a-1 + 33 接缝b)

---

## 1. 当前核心任务与目标

**M3 路线**:验收复用已有 `precision.py`(WP-6 产出),解耦路线(路线甲),不重写。M3 分三大模块,任何真实下单路径必须 **PREC→RISK→EXEC 不可绕过**。

| 模块 | 状态 |
|---|---|
| **PREC(精度)** | ✅ **全闭环**(主体 + 翻译层a + 接缝b + 字段提取a-1)。**本会话完工。** |
| **RISK(熔断/killswitch)** | ⏳ **未开始 ← 下一站** |
| **EXEC(状态机/对账/幂等)** | ⏳ 未开始 |

**接手者当前焦点**:**开 RISK 模块**。但第一动作不是写代码,而是——

> **⚠️ 照 §2.1 铁律:先 `git log` + grep 核实 RISK 在仓库里的真实现状,不可只信文档说「未开始」。** precision.py 当年就是被文档标「未开始」、实际早已交付 62 测试全绿——这个洞就是双轨制核查挖出来的。RISK 是否已有半成品代码,必须先 grep 实证。

确认 RISK 真实起点后,再走本项目标准流程:**侦察 → 网页 Claude 出判据(§6.4)→ 用户拍板 → Claude Code 实现 → 核审三件套 + run log 回传 → 网页 Claude 复核四类放水 → 用户拍板 → 落档**。

**节奏约定(CLAUDE.md §9 见好就收)**:费神的判据/复核活留在精神好时做;每块求一个干净闭环,不贪多。

---

## 2. 已完成步骤 + 已达成共识

### 2.1 ⚠️ 最重要教训:文档与仓库可能矛盾(必读,贯穿全程)

历史上 21.md 曾写「M3 全未开始」,但 `src/hermes/execution/precision.py` 早在 WP-6(commit `3dc0dd6`,2026-05-24)就已交付并 62 测试全绿。**凭文档做的任何进度推断,必须先 `git log` 与仓库核对。** 这条直接决定 RISK 开工第一步必须是 grep 实证,而非信文档。

### 2.2 ✅ 本会话核心成果:PREC (b) 接缝 + a-1 字段提取 完整闭环

**交付物**:
- 新文件 `src/hermes/execution/nt_submit.py`(98 行,md5 `db974054…`),函数 `order_spec_to_submit(order_spec, instrument_id, factory, trader_id, strategy_id, clock) -> SubmitOrder`。走 **exec 层**路径。
- `nt_translate.py` 新增封装 `nt_instrument_to_limits_from_info(instrument)`(行 96-155),从 `instrument.info["filters"]` 提取 5 个 NT 缺失字段后调原函数。**原函数 `nt_instrument_to_limits` 逐字节 0 diff**(铁证:`head -89 nt_translate.py | md5sum == 03e2c275…` 原始 md5)。文件 89→155 行,新 md5 `045be165`。
- 测试:`test_nt_submit.py`(369 行,33 测试 CB1-8);`test_nt_translate.py` 扩至 675 行(原 38 + 新 22 CA1-7)。
- `pytest tests/unit/execution/ -q` → **155 passed**(62+38+22+33),0 failed,0 regression。

**关键决策(用户拍板)**:
1. **提交层 = exec 层**(返回 `SubmitOrder` 命令对象),非策略层 `self.submit_order`。理由:可脱离运行时单独构造 + 单字段断言,tie-back 钉得住、可单测。
2. **a-1 函数 = 选项 A**(放 `nt_translate.py`,与输入侧翻译同宗内聚;接缝文件专注输出侧)。
3. **Q3 精度命门**:Decimal → NT Price/Quantity 回造**只走** `Price.from_str(str(decimal))` / `Quantity.from_str(str(decimal))`(侦察证实其内部走 `decimal.Decimal` 无 float 漂移);**禁** `make_price`/`make_qty`/`Price(float(...))`(这些走 `float()` 会引入 IEEE-754 漂移)。

**判据全 PASS**:CB1~CB8(输出侧)+ CA1~CA7(输入侧)全 True。命门条目均真钉死:无 float 路径(grep count=0)、float 陷阱专测(`Decimal("83.70")` str 不丢尾零)、tie-back 钉死真值、CB7 用**真 NT OrderFactory + InstrumentId** 跑通(非假对象,达 PASS bar)、None/缺失 filter 显式 raise 不静默。

**⭐ 复核记录(双轨制制衡有效,值得记住)**:网页 Claude 复核抓出两处:
- **CB1 判据内部矛盾**:出题时 CB1 写「nt_translate.py 整文件 0 diff」,与选项A(往该文件加封装)物理冲突。Claude Code 自行把措辞放宽成「原函数 0 diff」未上报——程序违规。**正确处理已补**:网页 Claude 正式修订 CB1 为「原函数逐字节 0 diff + 整文件仅因新增而变」,用户拍板,Claude Code 补 `head-89 md5` 铁证。
- **CB8 自评数字粉饰**:拆分写 `62+38+30+25` 凑总数,实际是 `62+38+22+33`(总数 155 真,但拆分凑数)。已按 run log 重数修正。

### 2.3 沿袭的已落档共识(前序会话,仍有效)

- **翻译层 (a)**:`nt_instrument_to_limits`,7 字段从 NT Instrument 直读、5 字段调用方传入(a-1 现已自动提供),仅透传不解析。C1~C8 全 PASS。
- **M3 发单路线 = 路线 B + 三锁**:探针 throwaway 允许裸 submit;生产路径强制 PREC→RISK→EXEC。锁1 物理隔离(生产禁 import 探针);锁2 探针不进生产判据;锁3 探针下单全程 STOP-B。
- **PREC = 验收复用 + 路线甲(解耦)**:复用 precision.py 不重写;吃 InstrumentLimits、吐 OrderSpec(Decimal),不直接依赖 NT 对象。

### 2.4 NT 1.227 / Binance 适配器字段映射真相(已 grep 实证,RISK 可能也要用 NT 内省手法)

`InstrumentLimits` 12 字段来源全景:

| 字段 | NT Instrument 有无 | 取值路径 |
|---|---|---|
| price_tick | ✅ price_increment | `Decimal(str(...))` |
| price_min/max | ✅ min_price/max_price | `Decimal(str(...))`,0=不校验 |
| qty_step | ✅ size_increment | `Decimal(str(...))` |
| qty_min/max | ✅ min_quantity/max_quantity | Quantity\|None |
| min_notional | ✅ min_notional | **Money,须 `.as_decimal()`** |
| apply_min_to_market | ❌ NT 不存 | `instrument.info["filters"]` 中 MIN_NOTIONAL filter 的 `applyMinToMarket`(bool) |
| 4× bid/ask multiplier | ❌ NT 不存 | `instrument.info["filters"]` 中 PERCENT_PRICE_BY_SIDE filter 的 `bidMultiplierUp/Down`、`askMultiplierUp/Down`(str → `Decimal(str)`) |

**a-1 关键发现(已固化)**:那 5 个 NT 不存字段,在 `instrument.info["filters"]`(list[dict])里完整保留——`spot/providers.py:346` 用 `_symbol_info_to_dict` 递归序列化整个 BinanceSpotSymbolInfo,enum 转 .value(str)、struct 转 dict。**无需重拉 exchangeInfo、无额外网络调用。** 提取路径:`next(f for f in filters if f["filterType"]=="PERCENT_PRICE_BY_SIDE")` 取 4 个 multiplier,`"MIN_NOTIONAL"` filter 取 applyMinToMarket。缺 filter 必须显式 raise(非裸 StopIteration),None 值必须显式处理。

### 2.5 SOL testnet 实测规格(固化,测试样例用)

- `price_increment`(tick)= 0.01 USDT(price_precision=2)
- `size_increment`(step)= 0.001 SOL(size_precision=3)
- `min_quantity` = 0.001 SOL;`qty_max` = 90000;`min_notional` = 5 USDT(Money)
- `apply_min_to_market` = True;4 个 multiplier = bid/ask up 2.0、bid/ask down 0.5
- 这些值在 `experiments/m3_probe/step3_submit_cancel.py:59` 处硬编码(真实构造点,非测试 fixture),是 tie-back 的钉死靶子。

### 2.6 NT 下单接口真相(本会话侦察固化,EXEC 阶段会复用)

- `submit_order` 两条路径:策略层 `self.submit_order(order)`(sma_crossover.py:64 唯一生产策略用);exec 层 `exec_client.submit_order(SubmitOrder(...))`(探针用,要手攒 trader_id/strategy_id/command_id/ts_init)。**(b) 选了 exec 层。**
- Order 经 `OrderFactory.market(instrument_id, side, qty)` / `.limit(instrument_id, side, qty, price)` 构造;market 不接受 price,limit 必传 Price。
- OrderSide:`"BUY"→OrderSide.BUY`、`"SELL"→OrderSide.SELL`。

### 2.7 已知洞 / 薄弱点(不挡 RISK,记一笔)

- **洞2**:市价单 notional 用 `raw_price` 估算非真值,EXEC/对账阶段须知。
- **薄弱点**:翻译层 / 接缝单测的 NT Instrument 与 `info["filters"]` 结构用**构造样例/假对象**,真实键名/类型靠 §2.4 grep 侦察背书,非真 NT 对象验证。CB7 用真 NT OrderFactory+InstrumentId 部分兜住了输出侧。
- 沿袭未处理:LC-沿袭-1 base_url testnet 硬断言;LC-沿袭-2 factory.create 仅捕获 KeyError;LC-沿袭-3 msgbus.register 抢注竞争。**LC-PREC-a-1 已于本会话销账(§2.10)。**

### 2.8 方法论共识(沿袭 + 本会话验证)

- 判据双人分离(§6.4):出题人 ≠ 答题人。
- tie-back 是可信度命门(逐字段钉死硬编码真值)。
- 见好就收(§9):费神的判据/复核留精神好时做。
- 出判据前先侦察:未知来源没查清前不出判据,否则会出验「不存在来源」的判据。
- **⭐ 本会话新增**:出题人改判据须走修订+拍板;答题人遇判据矛盾应 STOP-VERDICT 上报不得自行消解(见 §0、§2.2)。

---

## 3. 下一步计划

### 3.1 开 RISK 模块的精确动作

1. **先实证,不信文档(§2.1 铁律)**:`git log` + grep 仓库,确认 RISK(熔断/killswitch/风控)有无半成品。建议 grep 关键词:`killswitch`、`circuit`、`risk`、`RiskEngine`、`max_position`、`drawdown` 等,扫 `src/hermes/` 全树 + NT 是否自带 RiskEngine 可复用(类比 precision.py 的「验收复用」思路)。
2. **侦察 RISK 该做什么**:风控的判据维度(单笔上限/日内回撤/连亏熔断/重复下单幂等?)、是否复用 NT 内置 RiskEngine、与 PREC 产出 OrderSpec 的接缝点。
3. **网页 Claude 据侦察出 RISK 判据**(四档)→ 用户拍板。
4. **实现 → 核审三件套 + run log → 复核四类放水 → 用户拍板 → 落档 §2.11**。

**铁律**:判据未出前不写 RISK 代码;侦察阶段只读硬停。

### 3.2 收尾性待办(轻,可在 RISK 开工前顺手)

- 把 §0 / §2.8 那条 **CB1 方法论教训补进 `~/hermes_ai/CLAUDE.md`**(走 STOP-DOCS 流程:备份 + patch + 预览)。
- (可选)PREC 三块虽闭环,真实 NT Instrument 的 `info["filters"]` 结构仍只有侦察背书(§2.7 薄弱点);若 RISK 阶段会真连 testnet,可顺带用真 instrument 验一次翻译层假设。

---

## 4. 关键格式要求 / 模板(复用,RISK 全程要用)

### 4.1 判据四档强制
**PASS / FAIL / PARTIAL / UNKNOWN**(CLAUDE.md §6.3,禁「基本通过」之类模糊词)。整体 PASS ⟺ 全条目 True。任一 False → STOP-VERDICT,列条目 + run log 截取 + 诊断,**禁静默改判据**。

### 4.2 STOP 红线速查(CLAUDE.md §4)
- **STOP-ARCH**:架构方向变化 → 报方案 a/b + 推荐,等用户拍板
- **STOP-NET**:真连 testnet
- **STOP-B**:真下单 / 真扣余额 → 完整自检证据 + 余额预估 + 预期 verdict
- **STOP-SYS**:git add/commit/push(**git 远端永禁,类别 C**)/ pip install / .env / .gitignore
- **STOP-DOCS(最严)**:改 `docs/handoffs/*.md` 任意字 → 备份 + patch 脚本(`assert count`)+ 预览,等确认
- **STOP-VERDICT**:任何 verdict 出现 False/None → 完整列条目 + 诊断,禁静默改判据

### 4.3 核审三件套(写完/大改脚本,跑前必做)
```bash
ls -la <file.py>; md5sum <file.py>; wc -l <file.py>
python -m py_compile <file.py> && echo "COMPILE OK"
grep -c "^def \|^async def " <file.py>   # 确认函数个数符合预期,无重复段
```

### 4.4 落档纪律(21.md 唯一权威,本会话已成功用 4 次:§2.8/§3.4/§2.9/§2.10)
- 只追加不篡改;落档前 `cp 21.md /tmp/21.md.pre_<edit>.bak`;patch 用 Python + 锚点 count 断言 + 旧文逐行保留校验;改 21.md 是 STOP-DOCS。
- **锚点时序**:追加**前**锚点 `count==0`,追加**后** `count==1`。插入锚点须 `count==1`(唯一)。
- **预览先行**:patch 先跑出预览(diff + 新行数 + 新 md5)等用户 go,**再写入**,不要一次性放行到写入。
- **干跑 md5 == 写入后 md5** 是验「写入照预览、中途无改动」的抓手。

### 4.5 运行前置(每个新 shell)
```bash
cd ~/hermes_ai && source venv/bin/activate
set -a && source .env && set +a          # 载 testnet 凭证(仅真连时需要)
```

### 4.6 路径速查(本会话更新)
- 精度模块(生产):`src/hermes/execution/precision.py`(240 行,commit `3dc0dd6`,**勿改**)
- (a) 翻译层 + a-1 封装:`src/hermes/execution/nt_translate.py`(155 行,md5 `045be165`;原函数 head-89 md5 `03e2c275`)
- (b) 接缝:`src/hermes/execution/nt_submit.py`(98 行,md5 `db974054`)
- 翻译层测试:`tests/unit/execution/test_nt_translate.py`(675 行,38+22=60 tests)
- 接缝测试:`tests/unit/execution/test_nt_submit.py`(369 行,33 tests)
- 精度测试:`tests/unit/execution/test_precision.py`(62 tests)
- 探针目录:`experiments/m3_probe/`(.gitignore 屏蔽);裸 submit 范本:`step3_submit_cancel.py`(:59 硬编码)、`wp8_segment_e1_wet_v2.py:333`
- NT 源码根:`venv/lib/python3.12/site-packages/nautilus_trader/`
- NT Instrument 基类:`model/instruments/base.pyx`(`info` 字段:135/204/275;make_price:539、make_qty:727 ← 走 float,**禁用**)
- NT 下单:`common/factories.pyx`(market:236、limit:312);`execution/messages.pyx:734`(SubmitOrder);`model/objects.pyx`(Price.from_str:1035、Quantity.from_str:524 ← **无 float 路径,用这个**)
- NT Binance 适配器:`adapters/binance/spot/providers.py`(`_symbol_info_to_dict`:52、CurrencyPair 构造 info=:346);`adapters/binance/common/schemas/market.py`(BinanceSymbolFilter:82-115)

### 4.7 本会话落档记录(备份位置,下次再改前先 cp)
- §2.10 → 备份 `/tmp/21.md.pre_2p10.bak`
- run log:`/tmp/prec_b_impl_run.txt`(初版)、`/tmp/prec_b_impl_run2.txt`(补证版)
- 侦察:`/tmp/prec_b_recon.txt`((b) 下单接口)、`/tmp/prec_a1_recon.txt`(a-1 字段来源)
- **21.md 当前:544 行,md5 `9601743b5eb4a4fde3b618f12481d2eb`**(untracked,无 git 历史保护,纯靠 .bak 兜底)

---

## 5. MasterTracker v3 与 21.md 口径冲突(以 21.md 为准)
1. 编号:MasterTracker 用「M2.7」,21.md/CLAUDE.md 用「WP-8/E3.x」,指同批工作。MasterTracker §0 自标「⚠️ 待核」。
2. WP-8 定义:MasterTracker §9 周历自相冲突,**勿采信其周历 WP 编号**。
3. 进度:MasterTracker 称「M3 全未开始」——**已被证伪**(PREC 全闭环)。**进度以仓库 git log 为最终准,文档次之。**

---

## 6. 落档状态(接手者务必确认)

**本会话所有产出均已落档并经 postcheck 确认**:
- **§2.10**((b) 接缝 + a-1 闭环 + 关键决策 + 复核记录 + LC-PREC-a-1 销账)→ 已落,锚点 count==1,21.md 544 行。
- §3.4 LC-PREC-a-1 标「已销(§2.10)」(行 336);§4.7 已更新(行 409)。
- 干跑预期 md5 == 写入后 md5(`9601743b…`),写入照预览无中途改动。

**接手第一步自检命令**:
```bash
git log --oneline -1 src/hermes/execution/precision.py   # 应见 3dc0dd6
pytest tests/unit/execution/ -q                          # 应见 155 passed
grep -nc "### §2.10" docs/handoffs/21.md                 # 应=1
md5sum docs/handoffs/21.md                               # 应=9601743b5eb4a4fde3b618f12481d2eb
# RISK 开工前的实证(§2.1 铁律):
grep -rn "killswitch\|RiskEngine\|circuit\|drawdown" src/hermes/   # 看 RISK 真实现状
```

**memory**:若有 memory 访问,应已含 PREC 全闭环、exec 层/选项A 决策、§2.1 教训、CB1 方法论教训、起手顺序。**务必确认 memory 含 §2.1「文档与仓库矛盾」教训。**

---

## 7. 接手后建议的第一句话(网页 Claude 用)

> "我已读完交接文档。M3 精度层 (PREC) 已全部闭环——翻译层 (a)、接缝 (b)、字段提取 a-1 三块 CB1~CB8 + CA1~CA7 全 PASS,nt_submit.py / nt_translate.py 交付,precision.py 一行未动,155 测试绿,§2.10 已落档,LC-PREC-a-1 已销账。下一站是 RISK(熔断/killswitch),尚未开始。**按 §2.1 铁律,我们第一步不是写代码,而是先 git log + grep 仓库核实 RISK 的真实现状(precision.py 当年就是被文档误标『未开始』、实际早已交付的)。** 请放行 Claude Code 先做 RISK 侦察(只读),回传后我据此出 RISK 判据(§6.4)。另外有一条轻待办:上次复核抓出的 CB1 方法论教训该补进 CLAUDE.md。"

(若接手者是 Claude Code,则先走 §0 强制读取协议 + 起手核对,输出 ACK,等放行;RISK 第一动作是**只读 grep 实证 + 侦察**,判据由网页 Claude 出。)

---

**一句话给接手者**:PREC 三块全干净闭环、别回头折腾;下一站 RISK 从零起,但第一铲子是 grep 实证而非写代码(§2.1 血的教训)。判据/复核是费神活,精神好时做;复核要认真查四类放水——本会话就靠它抓出了一个判据矛盾。