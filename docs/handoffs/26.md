# 进度交接文档 — STOP-B Phase 3（平仓 SELL）

> 用途：本会话 context 已满，需转移到新聊天窗口。本文档供接手的 AI（担任「复核方」角色）无缝接管。
> 阅读顺序建议：先读本文档 → 再读 repo 内 `CLAUDE.md` + `docs/handoffs/25.md` → 再据「下一步」动手。

---

## §0 角色与红线（最重要，先读）

### 三方角色（不可混淆）
- **主理人**：人类，唯一授权方。一切「是否发单 / 是否改 src / 是否 commit」必须由其显式 `yes/go`。
- **复核方**：网页端 AI（即接手本文档者）。职责是出 PLAN、逐条核判据、挡住带病操作，**不亲自执行**。
- **执行方**：Claude Code（命令行 agent）。跑命令、写脚本、（经授权后）发单，**不得自行推断判据编号或抢跑**。

### 红线（承 CLAUDE.md / 25.md，不可逾越）
- **绝不** `git add / commit / push`，改动留工作区由主理人手敲。
- **STOP-B**：testnet 真发单须主理人明确授权。
- **STOP-ARCH**：五基线文件零改动（见 §4 表）。任何需动 src 的操作立即停、升级为独立授权一刀。
- **STOP-DOCS**：写 `docs/handoffs/*.md` 须「预览先行 → 贴全文供复核 → 等 go → 写入 → 写入 md5 == 预览 md5」。
- **不抢跑**（25.md §3 血汗教训）：收到「改后回贴再 go」类指令，必须先回贴改动等确认，不得直接写入。
- **不在高 context 下自行推断**（25.md §3）：CB 编号以复核方定稿为准；满 context 是硬停信号，尤其禁止满 context 下做写操作或发不可逆真单。

---

## §1 核心任务与目标

**STOP-B Phase 3 — 平仓 SELL**：在 Binance testnet 上发一笔 `market SELL 0.07 SOL`，平掉 Phase 2（§2.18）建立的 0.07 SOL 留底仓，使账户/NT 仓位回到 flat。

- 这是对 §2.18「STOP-ARCH 甲 留底」状态的**蓄意离场**（非漂移）。
- **离场意图已由主理人显式确认**（同意把留底 0.07 SOL 平掉、§2.18「甲 留底」主动结束）→ 新会话无需再问，复述即可。

---

## §2 已完成步骤 / 已达成共识

### 2.1 前置侦察与闸门（全 PASS，实测值）
| 项 | 实测值 | 状态 |
|---|---|---|
| G1 precision.py git hash | `3dc0dd6` | ✅ 符合预期 |
| G2 pytest tests/unit/ | 468 passed / 0 failed | ✅ 零回归（flaky 当次全绿） |
| G3 五基线 md5 | 逐行对 §4 表 | ✅ 零漂 |
| G4 21.md | `d20f38da…` / 960 行 | ✅ |
| G6 账户余额 | SOL 5.37 / USDT 9968.9706 | ✅ 符合 §2.18 终态 |

### 2.2 四项 VERIFY 已清
- **V1 = SPOT（现货）** → 无做空风险，`reduce_only` N/A，**CB17 已删除**；PnL 走现货口径。
- **V2 真实回调名（已锁）**：
  - 成交侧 `ExecBridge.on_order_filled(event: OrderFilled)` @ `exec_bridge.py:113`（`_handle_event` @:100 OrderFilled 分支）
  - guard 侧 `RiskGuard.on_trade_result(pnl: Decimal)` @ `guard.py:77`
  - Phase 3 走**路径 B**：`position_realized_pnl_extractor`，触发 `PositionClosed` 事件
- **V3 cost basis = 82.24**（真实成交价，非估算 82.23）。来源：PositionOpened `avg_px_open=82.24`。CB12 对账基准锁 82.24。
- **V4 = 零 src 改动**：`nt_submit.py` `_SIDE_MAP` 原生支持 SELL；`exec_bridge.py` 已覆盖 `PositionClosed`。脚本只改 `side="SELL"` + 捕获 `PositionClosed`，五基线零动 → STOP-ARCH 不触发。

### 2.3 判据 CB1~CB16 已封板（CB17 删除，SPOT）
关键非平凡条目（区别于 Phase 2）：
- **CB11 平仓 tie-back**：`fill.last_qty(0.07) == 平仓前 pos.net(0.07)` 且成交后 `net==0`（flat），依据 `PositionClosed`，`|diff|=0`，run log `[CB11]` 内联直验。
- **CB12 realized PnL（现货口径，取代 Phase 2 的 pnl==0）**：三分支断言
  - 盈利 `sell_px>82.24`：`realized_pnl>0` 且对账，guard 损失计数**不增**
  - 亏损 `sell_px<82.24`：`realized_pnl<0` 且对账，且 `_consecutive_loss_count` 较发单前 **+1**
  - 持平 `sell_px==82.24`：`reconciles and |realized_pnl|<=PNL_TOL`（容差 0.01 USDT）
  - 对账公式：`expected_pnl = (fill_px − 82.24) × 0.07 − commission_in_quote`
- **commission 毛/净已核实**：NT realized_pnl 是**净值**，但**仅扣同币种(USDT) commission；跨币种(SOL) commission NT 忽略不扣**。故公式只在 USDT commission 时减；Phase 2/3 commission 为 0 SOL，公式退化为 `(fill_px−82.24)×0.07`。
- **CB13/CB14**：`on_trade_result` / `on_order_filled` 离线 spy 补证 `called>=1`，**明确标注「离线补证，非 run log 内联」**（沿用 Phase 2 诚实标注法）。
- **CB16**：STOP-ARCH 离场登记「甲 留底」→「已平」。

### 2.4 脚本已写、已复核通过
- **文件**：`experiments/m3_probe/stopb_phase3_close.py`（.gitignored）
- **md5**：`04416ca26d29aa9fd4e223d99a1648f9` / **779 行**
- **复核方裁定**：脚本本体判据层面**复核通过**（CB11 abort 闸落地、CB12 三分支齐含 reconciles、commission 毛净已核实、md5 自洽）。
- **关键设计**：脚本内置 `pre_close_net == 0.07` 硬 abort 闸 —— 读不到 0.07 即 `disconnect → flush → return 1`，**不进 SELL 序列**（防 NT 把 SELL 记成开空）。

---

## §3 未解决问题 / 下一步计划（接手重点）

### 3.1 当前阻塞：reconcile 失效（核心未解决项）
**干跑验证实测结果**：
```
[INJECT-B] synthetic fill processed
[CB11-GATE] NT pre_close_net=0  (expect 0.07)
[ABORT-CB11] pre_close_net=0 != 0.07 — will NOT send SELL
returncode=1
```
- **现象**：option (b) 合成注入 `exec_engine.process(synth_fill)` **未生效**，`portfolio.net_position` 仍为 0（而非 0.07）。
- **结果**：abort 闸**正确触发，SELL 未发出**（STOP-B 维持）。→ 这验证了 abort 闸真的能拦住「NT net=0 时误发 SELL 变开空」的灾难，是本会话最有价值的产出。
- **留底 0.07 SOL 静止未动**，账户 SOL 仍 5.37。

### 3.2 已诊断的修复方案（STOP-ARCH 已确认不触发）
- **根因**：合成注入前，synth_order 状态机未走到能接收 `OrderFilled` 的状态。
- **修法**：在 `stopb_phase3_close.py` 内，用 NT 自带 `TestEventStubs` 给 synth_order 补 `submitted + accepted` 的 apply，使其到 `ACCEPTED(6)` 再注入 `OrderFilled`。
- **STOP-ARCH 判定**：**不触发**。修复点仅在 `.gitignored` 脚本内，五基线零动。`TestEventStubs` 在 NT test_kit 内，仅需新增一行 import。

### 3.3 复核方未结清的 Q1~Q3（改脚本前必须澄清）
该修法本质是**用 test_kit 合成事件把 net_position 顶到 0.07**（伪造成交历史），而非「reconcile 真实留底」。改脚本前须只读澄清，确认无语义污染：
- **Q1 cost basis**：synth_fill 喂入的 `avg_px_open` 必须显式 `== 82.24`（真实 Phase 2 价），否则 CB12 PnL 对账基准失真。
- **Q2 CB11 语义**：现 net 由 stub 顶出，CB11 实际验的是「fill.qty == stub 注入值」而非「对真实持仓」。须确认是否仍满足封板 CB11 语义，或 CB11 补注「net 经 test stub 合成，非真实 reconcile」诚实标注。
- **Q3 NT 记账 vs venue 真成交**：账户真有 0.07 SOL（5.37 含），SELL 0.07 是真卖。须确认 NT（靠 stub 合成）与 venue（真成交）两侧不打架（NT flat + venue 真卖 0.07）。

### 3.4 附带记录：flaky 时序（暂不处理，发单前纳入 PLAN）
- `test_reconnect_exponential_backoff` / `test_backoff_capped_at_60s` 高负载偶发失败。
- 脚本 CB2 判据 `passed>=468 and failed==0`，任一 flaky 即 ABORT。
- 建议：发单前连续 2~3 次 pytest 全绿后**立即**进，降低 CB2 误拦概率。

### 3.5 下一步执行序列（新会话）
```
1. 起手核对（只读）：脚本 md5 == 04416ca… / 五基线零漂 / 21.md d20f38da·960 / 25.md fff2a979·131 / 账户 5.37·9968.9706
2. 只读澄清 Q1~Q3 → 复核方确认无语义污染
3. 改 stopb_phase3_close.py（仅此文件，补 submitted+accepted apply）→ 出新 md5 → 回贴复核
4. 重跑干跑验证：确认 pre_close_net 真能 == 0.07（==0.07 才有意义继续；!=0.07 仍 abort，回炉）
5. pre_close_net==0.07 确认后 → 主理人 step3 go（离场意图已确认，复述即可）→ 才发 SELL
6. 发单后回传 run log + CB1~CB16 实测 → 复核方逐条核 → 落档（STOP-DOCS 预览先行）
```

---

## §4 关键背景 / 数据 / 格式要求

### 五基线 md5（STOP-ARCH 零漂基准）
| 文件 | md5 |
|---|---|
| `src/hermes/risk/guard.py` | `6d13bfb2608945f8ba9311b3d36bcfb5` |
| `src/hermes/execution/precision.py` | `bb0308ce6cf0b50f5214e5d9819f2cc8` |
| `src/hermes/execution/nt_translate.py` | `045be1651ba9b0f0561d66c8f3a10cd5` |
| `src/hermes/execution/nt_submit.py` | `db974054f08f4a15ee3357cd74a565cb` |
| `src/hermes/execution/exec_bridge.py` | `5b4453fc227a7da079c89f5446e35ffb` |

### 关键数据
| 项 | 值 |
|---|---|
| Phase 1 venue_oid（cancel，未成交） | 1201712 |
| Phase 2 venue_oid（BUY，+0.07 SOL） | 1203227 |
| Phase 2 fill last_px（= cost basis） | 82.24 USDT |
| Phase 2 trade_id | 281273 |
| 账户起手 SOL / USDT | 5.37 / 9968.9706 |
| Phase 3 脚本 md5 / 行数 | `04416ca26d29aa9fd4e223d99a1648f9` / 779 |
| precision.py git hash | `3dc0dd6` |
| 21.md md5 / 行数 | `d20f38dae6dcd32d0b96a187915f1152` / 960 |
| 25.md md5 / 行数 | `fff2a97960c5a446ece0cfd1f5f862bd` / 131 |
| pytest baseline | 468 passed / 0 failed（flaky 间歇） |
| PnL 容差 PNL_TOL | 0.01 USDT |

### 防认知污染（务必区分）
- **Phase 1** = cancel 路径，不成交，ΔSOL=0。
- **Phase 2** = market BUY，SOL +0.07 @ 82.24，留底（STOP-ARCH 甲）。
- **Phase 3（本任务）= 平仓 SELL，尚未发单。** 留底 0.07 SOL 仍在 testnet 账户静止。
- **当前无任何挂起单、无脏成交、无未完成 git 操作。** abort 闸已拦下一次潜在错单。

### 格式 / 工作流要求
- 判据用 `CB1~CBn` 编号，复核方定稿，执行方不得偏移。
- 指令块用 `[PLAN] / [RECON] / [REVIEW-REQ] / [REVIEW-VERDICT]` 等标签。
- 落档走 STOP-DOCS：预览先行，预览 md5 == 写入 md5，由主理人手敲。
- 回报实测值（hash/数字/md5），不接受裸「PASS」。
- LC backlog 暂不碰：LC-WS-1 / LC-EXEC-1/2/3 / LC-RISK-1。

---

## §5 给接手 AI 的一句话总结
脚本已封板复核通过，但 reconcile（让 NT 认得 0.07 留底）在干跑中实测失效（`pre_close_net=0`），abort 闸正确拦下了 SELL。下一步是在新会话干净 context 下：先澄清 Q1~Q3（伪造成交修法的语义污染风险）→ 改脚本（仅 `.gitignored` 脚本，不触 STOP-ARCH）→ 重验 `pre_close_net==0.07` → 才进发单序列。**离场意图已获主理人确认；发单仍需主理人在 pre_close_net==0.07 确认后显式 go。**