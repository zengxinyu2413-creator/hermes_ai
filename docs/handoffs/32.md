# 32.md -- 会话收口：E2.5-b3a BinanceLiveExecClient + b3b-wiring trade-live 接通 NT 管线（2026-06-02）

承接：docs/handoffs/31.md（E2.5-b1 翻译函数 + b2 CLI 骨架，锚 d1ab52b）。本文档为本会话收口，供下个会话无缝接手。

## 0. 背景定位
- 承接自：31.md（b1/b2 收口，锚 d1ab52b）
- 本会话产出：① 31.md 落库（commit fb6a09c）；② E2.5-b3a BinanceLiveExecClient + 19 mock 测（commit 3b10272）；③ E2.5-b3b-wiring trade-live 接通 NT 管线 + 5 wiring 测（commit 68cc341）；④ GATE-1 行为级复验 CLEAN（含触发模型坐实）

## 1. 红线（承 CLAUDE.md，不变）
- 绝不 git add/commit/push（CC 被 GATE-1 物理拦，主理人手敲）
- STOP-DOCS：预览先行 + 预览 md5 == 写入 md5；写后等显式 go 才 cp（签 PHASE1 != go，L1）
- STOP-B：连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐

## 2. 本会话核心产出
### docs：31.md 落库（fb6a09c）
- docs(handoffs): add 31.md session close-out。系上一会话收口文档落库（在 b3a 之前）

### E2.5-b3a：BinanceLiveExecClient（3b10272）
- 新建 src/hermes/execution/binance_exec_client.py：NT LiveExecutionClient 实现，底层托 BinanceRestClient
- 接口：_connect / _disconnect / _submit_order + event reporting
- liquidity_side 暂置 NO_LIQUIDITY_SIDE placeholder（待 b3b 填）
- 测试：19 mock 用例（不出网）
- 验收：644 passed（625 baseline + 19，零回归）；ruff/mypy delta=0

### E2.5-b3b-wiring：trade-live 接通 NT 管线（68cc341）
- 改 src/hermes/cli.py：trade-live 子命令从 b2 的 NotImplementedError 存根 → 接 NT 管线
- X 路决策：live path 用 NT 内置 BinanceLiveExecClientFactory；自建链 a(new_order)/b1(nt_order_translate)/b3a(BinanceLiveExecClient) 在 live path dormant（保留未删，未来去留待定，见 backlog 4）
- --dry：在 TradingNode 构造前 return（return 落在 node 构造之前，代码结构锁死）；零 NT kernel、零网络（HAS_NT_KERNEL_LOG=False 代码级坐实）
- 安全默认：environment=TESTNET 显式（缺省→mainnet，故显式写 TESTNET）；max_retries=0（发单零重试）；凭据走环境变量、不进 config（NT 读环境变量）
- factory 注册（测试 _invoke_live 验）
- 新建 tests/unit/execution/test_trade_live_wiring.py：5 wiring 测
- 判据 A–G 全 CLEAN：A env==TESTNET / B max_retries==0 / C --dry 不调 run（结构：return 在 TradingNode 构造前）/ D 凭据不进 config / E factory 注册 / F 649 passed + ruff All passed + mypy clean / G dry 零网络零 kernel（代码级）
- 验收：649 passed（644 + 5，零回归）；ruff/mypy delta=0
- scope 坐实：commit 仅 cli.py(M, 44±) + test_trade_live_wiring.py(新, +106)，2 files / 146 insertions / 4 deletions

## 3. 当前基线快照
- git 锚：origin/main = 68cc341；链 68cc341 → 3b10272 → fb6a09c → d1ab52b → bd75c77 → 916a820 → 533b1c2；工作树 clean
- baseline：649 passed
- 三闸指纹（用户级 ~/.claude/，不随 repo，环境重建须重施；全值承 30.md）：git-write-gate 281c070d / mainnet-gate 02ed4b83 / trade-gate c9c4cbb0 / settings 625c1a96
- 三闸行为（本会话）：
  - GATE-1：验活多次 —— wiring 自测 deny + HEAD 未动；诊断 T2(--allow-empty) deny + HEAD 未动；合并块预检整块 deny。触发模型坐实 = PreToolUse 预检 string-match（含 git-write 关键字整块在执行前被毙）。本会话 GATE-1 自测均 deny、无新空 commit 入链（异于 31.md 记的 916a820）
  - GATE-2：本会话未触碰
  - GATE-3：本会话未重验（wiring 只加 dry path，dry 在 node 构造前 return、不触发 trade-live 真路径）；b3b-live 起 node 时须重验「node.run / socket 打开前被 TRADE-GATE 拦」

## 4. 关键教训
- L8：假绿 commit —— 本会话首次 wiring commit，主理人误敲 b3a 的命令（add 已入库的 binance_exec_client.py / test_binance_exec_client.py、msg 写 b3a），致 add 零暂存 → `nothing to commit, working tree clean` → push `Everything up-to-date`，被误读为「done」。`nothing to commit` / `Everything up-to-date` 不是成功，是「add 错文件（多半误 add 已入库旧文件）」的红旗。「done」类自报须四样 raw 坐实方认：`git diff --cached --stat` 非空且 scope 匹配 → `[main <sha>]` → `push <old>..<new>`
- L9：GATE-1 触发模型须知方能正读 raw —— 模型 = PreToolUse 预检 string-match（含 git-write 关键字整块执行前被毙）。故读类命令（git status）可吐 "no changes added to commit" footer 而不触发 deny，勿误读为 gate bypass。糊/暧昧 raw 须以干净诊断坐实（status 基线 + --allow-empty 真写尝试 + HEAD 前后对账），不外推上轮干净自测、不 panic 定性闸坏（承 L7）

## 5. backlog（条件未齐，不动）
1. flaky test_backoff_capped_at_60s（test_binance_ws.py::TestReconnect）：计时依赖，须 quarantine（承 30/31.md）
2. B5 vacuous：无 telegram 模块，Telegram 建成后重验 SS6.8.2(3)（承 30/31.md）
3. CLI 层测试：b3b-wiring 已补 5 wiring 测（_invoke_live 等）；trade-live 真执行路径（去-dry）的测试待 b3b-live
4. 自建链 dormant 去留：a/b1/b3a 在 live path 弃用未删，留作 reference 还是后续接管/移除，待定

## 6. E2.5-b3b-live 进度 + 起手前置（下一步，未授权）
- a / b1 / b3a / b3b-wiring 完成（68cc341）
- b3b-live = 去掉 --dry、真起 testnet TradingNode + NT 内置 exec factory 连 Binance testnet + 打第一笔 testnet 真单（Category B，最高门槛）
- b3b-live 三件套（缺二）：
  - ① 发单闸 CLEAN —— GATE-3 须在 b3b-live 起 node 时重验「node.run / socket 打开前被 TRADE-GATE 拦」（wiring 已把 run 放在去-dry 路径，本会话未重验）
  - ② Iris 显式 STOP-B（未给）
  - ③ 复核方判据（待出，wiring 闭后可在 Iris 授 STOP-B 后出）
- 真打单参数（标的 / order_type / 数量 / testnet 余额）STOP-B 时定

## 7. 下个会话接手指引
1. 先复验三闸：SS6.7 + SS6.8.1（CC 主体），确认 GATE-1/2/3 仍活；指纹对账以第 3 节为准；GATE-1 触发模型见 L9
2. b3b-live 起手前必须：Iris 显式 STOP-B + b3b-live Phase 0 recon（去-dry 后 NT 内置 factory 起 node 的路径 + GATE-3 在 node.run/socket 前拦得住的重验设计）→ b3b-live PLAN → Iris go
3. STOP-DOCS cp 前必等显式 go（L1）；GATE 行为自测主体必 CC 且核 prompt 来源（L6）；「done」类自报四样 raw 坐实（L8）
4. 不读本文档就自驱推进是禁止行为
