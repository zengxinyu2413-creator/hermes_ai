# 30.md -- 会话收口:E2.5-a new_order 实现 + SS6.6 git-write-gate md5 绑定（2026-06-01）

承接:docs/handoffs/29.md（GATE-1/2/3 三闸建成验 CLEAN,，锚 e0034d7）、21.md（主进度）。本文档为本会话收口,，供下个会话无缝接手。

## 0. 背景定位
- 承接自:29.md（三闸 CLEAN,，锚 e0034d7）
- 本会话产出:① E2.5-a new_order + mock 单测（commit ed4bc76）;② SS6.6 补 git-write-gate md5 绑定（commit 9a562f3）
- 职责:收口 E2.5-a,，记三闸指纹 + 验收 + 教训 + backlog

## 1. 红线（承 CLAUDE.md,，不变）
- 绝不 git add/commit/push（CC 被 GATE-1 物理拦,，主理人手敲）
- STOP-DOCS:预览先行 + 预览 md5 == 写入 md5;写后等主理人显式 go 才 cp（签 PHASE1 != go,，L1）
- STOP-B:连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐

## 2. 本会话核心产出
### E2.5-a:BinanceRestClient.new_order（ed4bc76）
- POST /api/v3/order,，signed=True,，max_retries=0（发单绝不重试）
- Decimal->str 用 format(d,'f')（防科学计数法:Decimal("1E-8")->"0.00000001";不用裸 str()、不用 normalize()）
- 五校验:side in {BUY,SELL} / order_type in {MARKET,LIMIT} / quantity isinstance(Decimal) and >0 / LIMIT 必带正 price + tif in {GTC,IOC,FOK} / MARKET 拒 price;symbol .upper()
- timestamp/recvWindow/签名由 _request signed 路径注入,，new_order 不碰
- 测试:TestNewOrderMock 11 用例,，MockTransport 不出网;含小量回归 + 拒单(-2010 -> BinanceAPIError)
- 验收:全 unit 601 passed（590 baseline + 11,，零回归）;ruff/mypy delta=0

### SS6.6:git-write-gate md5 绑定（9a562f3）
- SS6.6 此前未绑 md5;现补 git-write-gate.sh md5 = 281c070df935338333d2fb18bfc74976
- 系新增绑定,，非改值。CC memory 中 ad6b8e17 非 CLAUDE.md 记录,，已 disregard;实测文件 == 交接基线 281c070d,，G1 行为复验 deny

## 3. 当前基线快照
- git 锚:origin/main = 9a562f3;链 9a562f3 -> ed4bc76 -> e0034d7 -> 6ebf093 -> 701cae6;工作树 clean
- CLAUDE.md:347 行,，md5 e886f31fb0169ae7ba62c47443e8cad6（含 SS6.6 md5 绑定 / SS6.7 / SS6.8 含 SS6.8.1-G3）
- 三闸指纹（用户级 ~/.claude/,，不随 repo,，环境重建须重施）:
  - git-write-gate.sh: 281c070df935338333d2fb18bfc74976（已在 SS6.6 绑定）
  - mainnet-gate.sh: 02ed4b830d12e93aa5968bf09501477e
  - trade-gate.sh: c9c4cbb0daf4299c62c391f26786614c
  - settings.json: 625c1a96856b32a9c50534f18305c9ef

## 4. 关键教训
- gate 指纹的"记录值"也须 raw 核,，不取 CC 转述。本会话 CC 跨多轮报"SS6.6 记录 ad6b8e17",，grep 一查 CLAUDE.md 从无该值（也无 281c070d）-- 系 CC memory 残留幻影。存在性/记录值类断言最不该用 verdict 替 raw。CC memory feedback_git_write_ops.md 若存旧 md5 是 stale,，待 CC 自清（不影响 repo）。

## 5. 未解决 backlog（条件未齐,，不动）
1. flaky test_backoff_capped_at_60s（tests/unit/exchanges/test_binance_ws.py::TestReconnect）:计时依赖,，偶发假 FAIL。须 quarantine（标 flaky 或修计时）,，否则 590/601 baseline 契约不可靠、会造假 STOP。
2. B5 当前 vacuous:src/hermes 全仓无 telegram 模块（零命中）,，未建。SS6.8.2(3) 空过;Telegram 建成后必须重验 B5。

## 6. E2.5-b 起手前置（下一步,，未授权）
- E2.5-b = exec client 接 NautilusTrader（BinanceExec client + new_order 接入 + testnet 起 TradingNode）
- 接入时按 SS6.8 带走项(1):trade-live 已注册进 GATE-3 hook deny;接 exec 层后 CC 主体跑 SS6.8.2(4) 行为级验闸 CLEAN（试跑启动命令须在 exec client 实例化 / socket 打开前被 TRADE-GATE 拦,，非 crash 兜底）
- STOP-B 三件套:发单闸 CLEAN（三闸 CLEAN;GATE-3 非-crash 重验待 exec 层）+ Iris 显式授权（未给）+ 复核方判据 -- 一件未齐

## 7. 下个会话接手指引
1. 先复验三闸:SS6.7 + SS6.8.1 验闸（CC 主体）,，确认 GATE-1/2/3 仍活;指纹对账以第 3 节为准
2. 接 E2.5-b:trade-live CLI + BinanceExec client -> 重跑 SS6.8.2(4) 行为级验闸 CLEAN（须被 TRADE-GATE 拦,，非 crash）
3. STOP-DOCS cp 前必等显式 go（L1）
4. 不读本文档就自驱推进是禁止行为
