33.md -- 会话交接：backlog 1 de-flake（修法 PLAN 已签，待 CC implement）（2026-06-03）
承接：docs/handoffs/32.md（E2.5-b3a + b3b-wiring + 32.md 收口，锚 6ec1e2a）。本文档为任务中途交接（非干净收口），供新会话无缝接手 backlog 1 的 implement 步。
0. 背景定位
项目：hermes —— 基于 NautilusTrader（NT）的 Binance 自动交易系统；当前在 E2.5-b 轨（exec client 接 NT + testnet 起单）
承接自：32.md（b3b-wiring 闭，锚 6ec1e2a；工作树 clean）
本会话已做：① 确认 32.md commit 落库（6ec1e2a）—— 上一会话收口闭环坐实；② 开 backlog 1 de-flake：recon 完成 + 根因诊断 + 复核方修法 PLAN 已签 CLEAN（尚未 implement、未 commit）
当前悬而未决 = backlog 1 的 implement + 验证 + commit（下一步，PLAN 已批，见 §5）
1. 红线（承 CLAUDE.md，不变）
绝不 git add/commit/push（CC 被 GATE-1 物理拦，主理人手敲）
STOP-DOCS：预览先行 + 预览 md5 == 写入 md5；写后等显式 go 才 cp（L1）
STOP-B：连交易所/真发单须 Iris 显式授权 + 复核方判据 + 发单闸 CLEAN 三者齐
2. 角色与流程约定（关键背景，新 AI 必读）
三方：CC（Claude Code，本机跑文件编辑/测试；git-write 被 GATE-1 物理拦）/ Iris = 主理人 = 用户（手敲 git commit/push，唯一 git 写入主体）/ 复核方（本对话角色：出 PLAN/判据、复核 raw、签 CLEAN，不碰执行）
节奏：recon → PLAN（复核方）→ implement（CC）→ 验证 raw（CC）→ 复核签 CLEAN → Iris 手敲 commit
raw 纪律：CC/Iris 的「done / CLEAN」是 verdict，git 读 / 测试输出是 raw，raw 翻 verdict（L7/L8）。复核方对存在性/状态类断言一律以 raw 为准、不取转述
3. 当前基线快照
git 锚：origin/main = 6ec1e2a；链 6ec1e2a → 68cc341 → 3b10272 → fb6a09c → d1ab52b → bd75c77 → 916a820；工作树 clean（backlog 1 改动尚未落地）
baseline：649 passed
三闸指纹（用户级 ~/.claude/，不随 repo，环境重建须重施；承 30.md）：git-write-gate 281c070d / mainnet-gate 02ed4b83 / trade-gate c9c4cbb0 / settings 625c1a96
三闸行为：GATE-1 上一会话验活（模型见 L9）；GATE-2/3 本会话未触碰；GATE-3 待 b3b-live 起 node 重验
4. backlog 1 详情（核心交接对象）
目标：de-flake tests/unit/exchanges/test_binance_ws.py::TestReconnect::test_backoff_capped_at_60s（计时依赖、会造假 STOP，污染 649 baseline 契约）
recon 坐实（已跑）：
用 fixtures no_jitter + sleep_calls；sleep_calls 把 sleep mock 成 await real_sleep(0)（只记 delay 值、零真实等待），no_jitter 锁 backoff 序列确定 → 断言走记录的 delay 值、非墙钟
测脚本：10× ConnectionClosedError + 1× hang，_collect() 收 11 条消息；外框 asyncio.wait_for(_collect(), timeout=2.0)
R7 实测：单跑 1.87s，loop 5x 最慢 2.23s（total）—— _collect() 自然完成时间已逼到 2.0s harness 上限
根因：Py3.12 asyncio.wait_for 改为 inline 调度（无 sub-task，同文件 test_zero_message_loss_across_reconnect 的 docstring 有载）× 11 reconnect cycle 的 event-loop 真实开销 ≈ 1.87s，撞 2.0s 边沿；慢机/CI 一旦 _collect() > 2.0s → TimeoutError → 假 FAIL。机制 = 实测裕度过薄，非某估值
已签修法（复核方 CLEAN）：
asyncio.wait_for(_collect(), timeout=2.0) → timeout=10.0（≈5× 自然完成；happy path 仍 ~1.9s 自然返回、收满 11 条即返回，不会拖到 10s）
该行上方加 1 行注释（说明 Py3.12 wait_for inline 开销 × cycle 数，harness 上限须 >> 自然完成时间，勿调回 2.0）
不 quarantine、不 skip、不 freezegun：sleep 已 mock、断言是真断言、event-loop 开销冻不住 —— 三者都不对症
scope 严格：只改 test_binance_ws.py 单文件
5. backlog 1 下一步（PLAN 已批，新会话直接执行）
CC implement（edit 走文件工具；验证走 Bash 工具块，L6 标主体；回传 raw 后 STOP）：
1.改 timeout 2.0→10.0 + 注释，单文件，不碰 src、不加 skip/marker、不动别的测
2.验证五读： 
oD1 sed dump 整个 test 体（确认 assert 走 delay 记录值、与墙钟无关 → timeout 非 load-bearing）
oD2 git diff tests/unit/exchanges/test_binance_ws.py（须只见 timeout 行 + 注释，单文件；多一字 abort）
oD3 loop 15x（回归绿——本机本就过、薄裕度未破，loop 只证无回归；de-flake 真凭是逻辑：上限抬到 5×、慢机不再撞顶）
oD4 全量 pytest -q（须 649 passed 零回归，baseline 不变）
oD5 ruff All passed + mypy delta=0（项目既定调用）
3.复核方复核 D1–D5 raw → 签 CLEAN → Iris 手敲 commit（scope = test_binance_ws.py 单文件，L8 四样 raw 坐实） 
o建议 msg：fix(test): widen reconnect-backoff harness timeout 2.0->10.0s (de-flake, Py3.12 wait_for)
baseline 账：保 649 不变（比 quarantine 强：没丢断言、没缩 baseline）；commit 后 backlog 1 转 resolved
6. 其余 backlog（条件未齐，不动）
1.B5 vacuous：无 telegram 模块，Telegram 建成后重验 SS6.8.2(3)（承 30/31.md）
2.CLI 层测试：b3b-wiring 已补 5 wiring 测；trade-live 去-dry 真执行路径的测试待 b3b-live
3.自建链 dormant 去留：a/b1/b3a 在 live path 弃用未删，留 reference / 后续接管 / 移除待定
4.（新观察，未立项）test_reconnect_exponential_backoff 的 timeout 数值或同病薄裕度 —— 若证实，另开一轮单独 de-flake，勿与 backlog 1 捆
7. b3b-live（最高门槛，未授权）
a / b1 / b3a / b3b-wiring 完成（68cc341）；b3b-live = 去 --dry、真起 testnet TradingNode + NT 内置 BinanceLiveExecClientFactory 连 Binance testnet + 打第一笔 testnet 真单（Category B）
三件套缺二：① 发单闸 CLEAN（GATE-3 起 node 时重验「node.run / socket 打开前被 TRADE-GATE 拦」）② Iris 显式 STOP-B（未给）③ 复核方判据（待 STOP-B 后出）
真打单参数（标的 / order_type / 数量 / testnet 余额）STOP-B 时定
STOP-B 从 Iris 发起，复核方不自驱、不替起头
8. 关键教训（承 30–32.md）
L1 STOP-DOCS：预览 md5 == 写入 md5、等显式 go 才 cp
L6：GATE 行为自测主体必 CC，须核 prompt 来源（落 ● Bash 工具块、非 Iris shell prompt；Iris shell 对闸假绿）
L7：见「闸该拦没拦」先分清执行主体，勿把 Iris-shell 假绿读成红线事故
L8：假绿 commit —— nothing to commit / Everything up-to-date 不是成功、是「add 错文件（多半误 add 已入库旧文件）」的红旗；「done」类自报须四样 raw 坐实：git diff --cached --stat 非空且 scope 匹配 → [main <sha>] → push <old>..<new>
L9：GATE-1 = PreToolUse 预检 string-match（含 git-write 关键字的整块在执行前即被 deny）；故读类命令（git status）可吐 "no changes added to commit" footer 而不触发 deny，勿误读为 gate bypass。糊/暧昧 raw 须以干净诊断坐实（status 基线 + --allow-empty 真写尝试 + HEAD 前后对账）
9. 新会话接手指引
1.读本文档全文（不读就自驱推进是禁止行为）；如可检索，并读 30/31/32.md 补全 E2.5 全貌
2.先复验三闸：SS6.7 + SS6.8.1（CC 主体），指纹对账以 §3 为准；GATE-1 模型见 L9
3.直接接 backlog 1 §5：CC implement → D1–D5 raw → 复核签 CLEAN → Iris 手敲 commit
4.backlog 1 闭后再问 Iris 方向（其余 backlog / 或 Iris 给 STOP-B 起 b3b-live）
5.STOP-DOCS / STOP-B / L6 / L8 全程在身；commit/push 永远 Iris 手敲
